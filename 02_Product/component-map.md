# Component Map

## Layout & Grid
Source: `app/styles/tokens.css`, `app/styles/grid.css`. Preview/vet at `app/frame.html`.

**Spacing scale** (also drives type line-height): 0, 4, 8, 16, 24, 32, 40, 48, 56, 64, 72. 4px is the only fine-adjustment step; everything above it moves in 8s.

**Grid:** 4 columns mobile, 8 tablet, 12 desktop (Material's standard layout-grid split — these three counts share clean factors, so half/quarter spans divide evenly everywhere; thirds only divide evenly at 12, so treat thirds as desktop-only).
- Mobile (< 600px): 4 columns, margin 16px, gutter 16px
- Tablet (600–1023px): 8 columns, margin 48px, gutter 16px
- Desktop (≥ 1024px): 12 columns, margin 72px, gutter 24px, content capped at 960px max-width (centers on larger screens)

Margins got a real bump at tablet and desktop (were 24px/32px) and max-width came down from 1200px — the page read close to edge-to-edge in practice, "like 10 columns" by feel even though the underlying grid was still technically 12 at desktop. `--grid-columns` itself wasn't touched (still 4/8/12) — changing that would break the "half always means half" contract `.col-half`/`.col-quarter`/`.col-third` depend on. The narrower, more-framed feel comes entirely from `--grid-margin`/`--grid-max-width` instead.

Because column count itself changes per breakpoint, spans are semantic classes, not fixed numbers: `.col-full`, `.col-half`, `.col-quarter`, `.col-third` (desktop-only, falls back to full width below 1024px). Each carries its own correct span at every breakpoint — e.g. `.col-half` is 2/4 → 4/8 → 6/12.

**Base reset** (`grid.css`, `button, input`): `appearance: none`/`-webkit-appearance: none` plus `font: inherit`/`color: inherit` — a real, previously-missing gap (nothing in the app had ever reset native button chrome), worth keeping regardless, but it turned out not to be the actual fix for "Add a show" reading off-center; see the Add show card entry below for the real cause and fix, confirmed by rendering the button headless and reading actual `getBoundingClientRect()`/text-ink positions rather than guessing from a screenshot.

**Type scale:** six sizes now, two faces. Big Shoulders Display is headline-only (900 weight, pinned explicitly) — it read as too loud on buttons/dates/avatars/labels, so data and body both moved to Public Sans, told apart by size/case/tracking rather than a typeface swap. Controls (12px) turned out too small once they were carrying every button in the app, not just dates/labels — split into its own size. Line-heights pulled from the spacing scale so type rhythm and layout rhythm are the same rhythm.
| Size | Line-height | Font | Use |
|---|---|---|---|
| 12 | 16 | Public Sans, uppercase + tracked (data) | timestamps, counts, meta labels |
| 14 | 16 | Public Sans, uppercase + tracked, semibold (control) | buttons, links, tappable controls |
| 16 | 24 | Public Sans (body) | default reading size |
| 20 | 24 | Public Sans, semibold (body) | card titles |
| 24 | 32 | Big Shoulders Display, 900 | section headings |
| 40 | 48 | Big Shoulders Display, 900 | hero numbers, page title |

**Color:** dark now, deliberately (an earlier dark-page attempt was reverted as a misread of an ambiguous request; this is a distinct, specific ask). `--color-bg` is charcoal (`#211f1a`) — the page itself, not a chip color living on a light page. `--color-surface` (`#2b2924`) is the card + drawer fill — one step lighter than the page, staying in the dark register (the opposite move from the old light-mode "cards lighter toward white"). `--color-surface-text` (`#ebe8e1`, warm paper) is text/icons on `--color-surface`; `--color-text` is the same paper value at the page level. Flipping the page to dark broke the old "dark chip on a light page" trick for filled/active elements (buttons, the calendar weekday header — the calendar was later cut entirely in favor of the Herd Picks/My Shows tabs) — a charcoal chip on an already-charcoal page is invisible — so those now use two new tokens instead: `--color-chip-bg` (`#ebe8e1`, paper) / `--color-chip-text` (`#211f1a`, charcoal), the same duotone logic just inverted polarity. `--color-text-inverse` stays pinned to white regardless of any of this — it's specifically "white text for a colored/dark fill" (avatar dots, the overlap badge), independent of which way the page itself goes. `--color-border` moved from solid charcoal to a translucent paper hairline (`rgba(235, 232, 225, 0.18)`) — a solid charcoal border would vanish on a charcoal page. Pink accent (`#f3596e`) is now unified across the app — overlap marker, wordmark "Gig," and the header avatar's hover state all share this one value (`--color-logo-pink` is just an alias for `--color-accent` now, not a separate value). This relaxes the earlier "pink means overlap, nowhere else" rule on purpose — see `brand-brief.md` for the full history of why. "Herd" keeps its own dedicated `--color-logo-green` (`#8dc63f`), still pinned to the London Calling / Elvis Presley cover homage — see Header nav below. No glow anywhere in the app; a glow pass on the wordmark's green half was tried and cut.

**Measure rule:** body copy targets 60–75 characters per line. On desktop, full-width (12 columns) runs too long — `.col-half` lands closer to the right measure. Use `.prose` (max-width: 65ch) alongside it for any real paragraph so the cap holds regardless of column width.

**Discipline:** every color, spacing, and type value in a component should reference a token from `tokens.css` (`var(--...)`). If a value doesn't exist as a token and needs to be reusable, add it to `tokens.css` first rather than hardcoding — flag it here when that happens so the scale stays intentional, not sprawling.

**Token audit — main screen spacing pass.** A direct request to clean up main-screen spacing and double-check token usage turned up a real, previously-invisible bug and a cluster of un-tokenized values:
- **`<p>` margin leak.** `.header-tagline` and `.footer-copyright` are both `<p>` tags with no `margin-bottom` (or, for the footer one, no margin at all) set explicitly — meaning each was quietly inheriting the browser's own default `<p>` margin (~16px, unrelated to any token), stacking on top of the header's own `--space-24` padding and `.show-list`'s `--space-32` top padding to make the gap under the tagline noticeably bigger than the design actually specifies. Found by rendering the header headless and reading the *computed* margin directly (`getComputedStyle(...).marginBottom` returned `16px`, not `0`) rather than guessing from a screenshot. Both now zero this out explicitly.
- **Duplicate rgba() literals, now `color-mix()` tokens.** `rgba(33, 31, 26, 0.45)` (the flat scrim, used identically by both the drawer and modal overlays) and four separate `rgba(235, 232, 225, …)` hover/active tints were all hand-typed decimal RGB re-derivations of colors that already had hex tokens (`--color-bg`, `--color-text`) — a second, unlinked source of truth for the same color that could drift out of sync silently. Replaced with `--color-scrim`, `--tint-hover`, `--tint-active`, `--tint-active-strong` in `tokens.css`, each built with `color-mix(in srgb, var(--color-bg-or-text) N%, transparent)` so they derive from the existing hex tokens instead of duplicating them. Also collapsed two near-identical hover opacities (0.06 and 0.08, used in different components for what was functionally the same "subtle hover" state) down to one shared value.
- **Off-scale magic numbers moved onto the scale or given a named token.** `.show-list-empty-icon`'s `56px` now reads `var(--space-56)` (already on the spacing scale — just wasn't being referenced). `.group-switch-item-count`'s `margin-top: 2px` — finer than the spacing scale's own "4px is the only fine-adjustment step" rule — became `var(--space-4)`. The wordmark's deliberately off-scale `30px` (documented as intentional — a step above `--text-head-size` but under `--text-display-size`) is now at least a named token, `--text-wordmark-size`, instead of a bare number sitting in `components.css` with no token backing at all.
- **Left alone, deliberately:** `.name-gate-box`'s `max-width: 400px` and `.modal-box`'s `max-width: 360px` — one-off content-width constraints, not part of the `--space-*` rhythm's contract (similar in kind to `--grid-max-width` itself being a standalone hardcoded value with its own token, not a multiple of the spacing scale).

**Fields — filled, not outlined.** Every text input in the app (`.form-row input` — name gate, Add Show's manual form, venue search) switched from a `1px solid var(--color-border)` outline on a background matching the page (`--color-bg`) to a real fill: `--color-input-bg`, a new token, one shade darker than `--color-bg` (`color-mix(in srgb, var(--color-bg) 80%, black)`). Worth naming: every other fill token in the palette goes *lighter* than `--color-bg` (`--color-surface` for cards, `--color-chip-bg` for active/filled buttons) — this is the one deliberate exception, chosen so a field reads as a recessed slot cut into the page rather than a raised surface sitting on top of it, which is what a lighter fill would have implied. Border dropped to `1px solid transparent` at rest (kept in the box model so nothing shifts on focus) — the fill itself is the field's boundary now; the existing accent focus ring stays a real outline, since "you're typing here" is a different signal from the field's resting shape. Padding also grew from `var(--space-8)` to `var(--space-8) var(--space-16)` — a bare `--space-8` all around read cramped once the field had real visual weight instead of just a thin outline.

**Icons — Lucide (lucide.dev), adopted as the app's standard iconset per explicit direction, starting with the share feature below.** Loaded from a pinned CDN build (`https://unpkg.com/lucide@1.29.0/dist/umd/lucide.min.js` — pinned per Lucide's own docs, not `@latest`, so an upstream release can't silently rename/change a glyph out from under the app), same external-CDN pattern the app already used for Supabase. Usage: plain `<i data-lucide="name"></i>` placeholders in the markup, swapped for real inline SVG by `lucide.createIcons()` at runtime. Two call sites, because `createIcons()` only replaces elements that exist in the DOM the moment it runs: a single top-level call (right before `init()` in the inline script) covers every icon already in the static HTML at load (`.drawer-close-btn`, `.modal-close-btn`, `.share-group-btn`); a second, scoped call (`lucide.createIcons({ root: drawerGroupList })`) sits inside `renderGroupSwitcher()`, re-run every time that list rebuilds, since each render wipes and regenerates its own `data-lucide` placeholders. `--icon-size` (`tokens.css`, 20px) is the one shared size for every icon-only control's SVG — button *boxes* still vary (`--space-40` for standalone circular controls, `--space-32` for the tighter in-row share button) but the glyph itself stays one consistent size everywhere, the same "box sizes the tap target, not the glyph" convention the old text-glyph buttons already used. Migrated so far: the drawer's ✕ and the people-modal's ✕ (both now `x`), plus every share affordance (`share`, flashing to `check` on successful copy — see `flashShareSuccess()`). Deliberately **not** migrated yet: the Add Show `+`/rotate-to-`×` toggle (its 45°-rotation trick is a plain CSS `transform` on the existing glyph span and works identically with an SVG child, but it's untouched for now to avoid risk to an already-tuned mechanism without a specific reason to touch it) and the empty-state guitar silhouette (hand-built from filled shapes; Lucide has no equivalent figurative icon). Revisit both if/when there's a reason to, rather than migrating for its own sake.

## Pages
- **Gig Herd — real app shell** (`app/index.html`) — the page being built up component by component on the frame. Currently: header nav + body container (show list) + nav drawer (which now also carries the footer content — see Drawer footer below).
- Gig Herd — original prototype (`app/on-the-radar.html`) — kept as reference; the mocked interactivity (list/calendar views, response states) it demonstrates gets rebuilt as real components on the frame rather than ported as-is.

## Components
- **Name gate** (`app/index.html`, logic in `app/scripts/identity.js`) — the whole "onboarding" surface, and deliberately not a splash page: when a browser has no `otr_person_id` cookie, the header and main content stay hidden and a centered box shows instead (`.name-gate-center`, flex-centered both axes, `.name-gate-box` capped at 400px — same treatment for every step below). Replaces the old `window.prompt()`.
  - **Two paths, decided once at load** by whether `?g=<slug>` is in the URL (`inviteSlug`/`hasInviteSlug`, captured before `handleGroupJoinFromUrl` strips it). A cold-start visitor (no link) gets the generic tagline ("Line up shows with your pals") and a second field in the same form — Group name — so person + first group get created together in one submit, followed by a share screen right after: two full-width actions ("Copy invite link", "+ Add first show") front and center, a quiet "Skip to your group" link below for anyone who wants past both. "+ Add first show" reveals the real app with the Add Show panel already open, so this screen gives way to the normal list the moment a show actually gets added.
  - **Invite-link message — names the actual group, as a second line.** First pass overwrote the generic tagline entirely on the invite path — cut after direct feedback, since it left the brand tagline missing and the two ideas ("here's the app" / "here's what you're joining") reading as one line with no room to breathe. Now `#name-gate-invite-message` is a separate `<p>`, hidden by default, sitting *under* `#name-gate-tagline` (which stays "Line up shows with your pals." on every path, invite or not) with real margin between the two. `init()` runs a separate, early `getGroupBySlug(inviteSlug)` (purely for display, before the person has even entered a name — distinct from the `getGroupBySlug` call inside `handleGroupJoinFromUrl`, which only runs after submit and actually joins) and sets its text to `You've been invited to join "{Group Name}" to track shows together. Add your name and jump in.`, then shows it. Stays hidden if the slug doesn't resolve (stale/bad link) rather than claiming an invite that isn't real. Deliberately doesn't name *who* invited them — the link is one per group, not one per sender, so there's no reliably correct person to credit; a wrong name would read worse than no name (see `01_Strategy/decisions.md`, 2026-08-06).
  - **Joining a group via invite link**: `OTR.getGroupBySlug` + `OTR.joinGroup` add the person to that group and land them in it — works both for a brand-new person (right after the name gate) and a returning one opening a friend's link for a group they're not in yet. The URL param gets cleaned up after processing so a refresh doesn't reprocess it. This is the join mechanism, full stop — no separate "invite code" concept, the slug just lives in a shareable link instead of something typed in, since sharing happens over text/iMessage where a tappable link beats a typed code.
  - **Slug format — readable name, not a hash.** `groups.slug` used to be `schema.sql`'s own default, a random 12-byte hex string (`098aea86c5eb9dc6461b166f`) — private-by-obscurity, but it made every invite link and the URL bar itself look broken. `OTR.createGroup` now slugifies the group's actual name client-side (lowercase, non-alphanumerics collapsed to single hyphens — "The Usual Suspects!!" → `the-usual-suspects`) and passes it explicitly on insert, overriding that DB default. A same-name collision retries with `-2`, `-3`, ... on the table's unique-constraint violation (Postgres `23505`) rather than failing outright — a few groups sharing a name across different friend circles is expected at this scale. Existing groups keep their old hex slugs (no retroactive rename — would break already-shared links); only groups created from here forward get readable ones. Trades away some of the earlier "unguessable slug" privacy stance — see `01_Strategy/decisions.md`, 2026-08-06, for the reasoning and the accepted risk (someone who guesses a common group name and knows to try it could self-join, since `joinGroup` has no approval step).
  - **Restyle — one centered column, filled fields.** The wordmark, tagline, input fields, and Continue button are a centered block. Field labels (`.name-gate-box .form-row label`) went through a center-aligned pass and back — centered read fine while there was only ever one field in view, but with two stacked ("What's your name?" / "Group name") a centered question sitting dead over its own answer field read odd; labels are left-aligned again now, same as every other form in the app, while the fields/button themselves stay full-width. Inputs switched from outlined to filled — see **Fields — filled, not outlined** below; this is app-wide, not scoped to the name gate, since Add Show's manual form and the name gate share the same `.form-row input` class.
  - **Step 2 ("You're in, share the link") — cut.** Used to show a second screen after creating a first group, front-loading "copy invite link" / "add first show" before the app itself appeared. Cut per direct feedback — an unnecessary extra beat, and both actions were already reachable elsewhere (the group heading row's share icon, the drawer's per-group share icon, the Add Show card itself). Submitting the name-gate form now goes straight into the app in every case — cold start, invite-link join, with or without a group name — instead of only the invite-link path skipping the interstitial. `pendingNewGroup`, `nameGateStepShare`, and the three step-2 button handlers were removed along with it, not just hidden.
  - **Scroll reset on entry.** `enterAppFromGate()` now calls `window.scrollTo(0, 0)` right after swapping `display` on the header/main/footer, before `afterPerson()` runs. Per direct feedback — right after setting up a group, the page could load scrolled a bit, header out of view. The DOM swap alone doesn't reset scroll position; the browser keeps whatever `scrollY` the name gate was at (an on-screen keyboard nudging the page while typing a group name is one real way to get there), which can leave the new, usually-shorter app shell landing mid-scroll instead of at the top. Explicit reset rather than relying on the swap to happen to land at 0. Verified headless: scrolled a tall stand-in element to `scrollY: 900`, hid it, confirmed `scrollY` reads `0` immediately after.
- **Header nav** (`app/index.html`, logic in `app/scripts/identity.js`, styles in `app/styles/components.css`) — just the avatar trigger and wordmark now, one row (`.header-row`). Built and wired to Supabase — no more mock data.
  - Layout: `.header-row` is a CSS grid (`grid-template-columns: 1fr auto 1fr`), not flex — a flex-centered wordmark only lands center when both side elements are equal width. The group switcher used to occupy the third column (see below — it's moved to the drawer now); the column stays in the grid template regardless, empty, so the two `1fr` side columns keep the middle column, and the wordmark inside it, dead center either way.
  - **Optical nudge** (`.header-row .wordmark`, `margin-left: var(--space-8)`): the grid genuinely centers the wordmark at the row's exact mathematical midpoint — verified headless (real `getBoundingClientRect()` measurements, not a guess), landed exactly on-center at both mobile and desktop widths. Still read as pulled left, though: the avatar is a solid filled circle sitting flush in the far-left column, and while the mirrored right-hand column reserves the *same space*, it has zero *ink* — nothing there to visually balance the avatar's weight. Same optical-vs-geometric gap as the Add Show button's earlier fix, just caused by unequal visual weight between the flanks instead of an unequal bounding box. 8px was picked by rendering several candidate offsets (0/8/12/16/20px) headless and comparing screenshots — 8px read balanced without overshooting, and scales fine since its visual impact naturally shrinks as the row widens at larger breakpoints (confirmed at both 390px and 1024px). Scoped to `.header-row .wordmark` specifically — the name-gate's own wordmark has no avatar beside it, so it stays at true geometric center, no nudge applied.
  - Background: none — no fill, no border, no shadow. The header just sits on the page's own charcoal, airy rather than boxed off; extra `padding-block` (`--space-24`, up from `--space-16`) does the breathing-room work a border used to do.
  - Wordmark: links home, Big Shoulders Display, weight 900 (pinned explicitly), 30px — a step above `--text-head-size` (24px) so it carries real presence now that it's two-tone — centered. Tight-set with no space — `<span class="wordmark-gig">Gig</span><span class="wordmark-herd">Herd</span>` — pinned directly to the London Calling / Elvis Presley cover homage (see `brand-brief.md`): "Gig" is `--color-logo-pink`, now just an alias for `--color-accent` (`#f3596e`) — unified with the overlap marker, per explicit direction, after a few passes of separate near-identical pinks. (The header avatar's hover fill used to be pink too, in an earlier pass — since moved off it; see "Avatar trigger" and "Footer" below.) "Herd" is `--color-logo-green` (`#8dc63f`, chartreuse), its own dedicated token. Both flat fills, no glow (a glow pass on "Herd" was tried and cut — brand's "no glow, full stop" rule held). Worth flagging: this means "pink means overlap" is no longer strictly true anywhere in the app — see `brand-brief.md`. Everything else in the app stays black/charcoal — only the logo gets color.
  - **Avatar trigger** (`.header-avatar-btn`, `#hamburger-btn` — id kept for continuity, no longer literally a hamburger): the current person's own initials in a filled dot, colored with their own chosen avatar color (see "Avatar color," below) instead of a uniform charcoal. Opens the nav drawer, which now holds identity and group switching — everything "you"-related lives behind this one trigger.
    - **Hover — off pink.** Used to swap to `--color-accent` on hover, a real "pink means overlap, nowhere else" violation hiding in plain sight. Since the resting fill is a solid, per-person avatar color (not a fixed token), the usual `--tint-hover` translucent-overlay trick wouldn't read against it the same way a transparent-background hover does — so this uses `filter: brightness(1.15)` instead, a lighter tone of whatever color is already there, whichever one that is. `.btn-solid:hover` (Continue, group modal's invite button, etc.) got the same treatment in the opposite direction — `brightness(0.92)`, since a light chip reads better dimming slightly than trying to lighten a fill that's already close to white.
- **Nav drawer** (`app/index.html`, `#nav-drawer` + `#drawer-overlay`) — identity + avatar color, and the group switcher. Slides in from the left over a dimmed flat overlay (no shadow, same flat-only rule). Surface uses the same `--color-surface`/`--color-surface-text` tokens as a card — a raised panel, not a special-case dark surface.
  - **Redesign pass** (structurally Airbnb-inspired — generous, consistent spacing rhythm and every list row sharing one real card shape rather than an ad hoc mix): `.nav-drawer`'s own padding went from `--space-16` to `--space-24`; `.drawer-section`'s padding/margin went from `--space-16` to `--space-24` too, so the identity+color block and the groups block read as two clearly separate groups, not one continuous list. The section divider itself was the "harsh stroke" — it was a hardcoded `rgba(255, 255, 255, 0.15)`, not a token at all, so it rendered as a generic pure-white line instead of the app's own warm-toned hairline. Now uses `--color-border`, the same translucent-paper token used for the avatar-group divider elsewhere. `.drawer-close-btn` became a real icon button — a `--space-40` circular hit area with a hover fill, matching the header avatar trigger's own conventions, instead of a bare glyph with just padding and an opacity dim.
  - **Right-edge corner radius.** `.nav-drawer` sits flush against the screen's own left edge (nothing to round against there), so only the right corners pick up `var(--radius)` now (`border-radius: 0 var(--radius) var(--radius) 0`) — per direct feedback, matching the same corner radius every card in the app already uses, rather than the drawer being the one hard-edged rectangle left.
  - **Identity section — fill band, not a stroke.** `.drawer-identity-section` replaces the identity block's inherited `.drawer-section` bottom stroke with a `--color-fill-dim` band instead — same token/philosophy as the footer's own "darker recess" treatment (see Footer below), reused rather than inventing a second "darker" value. Bled out to the drawer's own edges via negative margins (cancelling `.nav-drawer`'s `--space-24` padding on all three non-bottom sides) and picks up its own `border-top-right-radius` so the band's corner matches the drawer's new right-edge radius above it. Deliberately kept at `--color-fill-dim` rather than something more aggressive, per direct feedback ("not to make it look weird against the main screen bg") — this token is already calibrated as a subtle recess relative to the page, not a hard block of contrast.
  - **Dual-tone identity glow.** Tried first behind the site header, then moved here per direct follow-up feedback ("no gradient in the header, I want it in the nav drawer header instead"). Came out of a round of mockups: radial glow picked over a horizontal stage wash and crossing light beams (both called "too cheesy"), then a follow-up pass adding a second color highlight for "a more nuanced gradient mapping." Three radial layers stacked as extra `background` gradients on `.drawer-identity-section`, on top of its own `--color-fill-dim` (the last layer in the list): a center glow near the avatar at the person's own `--avatar-color` (42% mix, the dominant light), plus two faint corner glows at 15% using the wordmark's own existing tokens — `--color-accent` (Gig pink) and `--color-logo-green` (Herd green) — not new one-off hex values. `--avatar-color` is set on `.drawer-identity-section` in `renderMyAvatarChrome()` (same custom property already applied to the drawer avatar and header trigger). No pseudo-element or `z-index` needed here, unlike the header attempt — background paint sits behind child content automatically, and it clips to the section's own `border-top-right-radius` the same way `--color-fill-dim` already did. Deliberate, called-out exception to the brand's "no glow, full stop" rule everywhere else — reserved for identity, not decoration.
  - **Close button — moved inside the band, not just above it.** `.drawer-top` (holding `#drawer-close-btn`) used to be a sibling row sitting *above* `.drawer-identity-section`, still inside `.nav-drawer`'s own padding — so the fill band visibly started below the close button instead of at the drawer's actual top edge, on the lighter drawer surface. Per direct feedback ("the header dark bg in nav needs to inhabit that whole space up to the top border"), `.drawer-top` is now the identity section's first child instead of a preceding sibling — its negative margin now pulls the close button up into the band along with everything else, so the fill genuinely covers the whole top of the panel, flush to the true top border.
  - Identity: current person's name + their own avatar dot, filled with their chosen color (text color computed per-swatch — see "Avatar color" below — rather than fixed white, since the palette now spans from near-white to near-black).
  - **Name + color editing — click the avatar, or the name.** `.drawer-avatar` (`#drawer-avatar`) went from a plain non-interactive div to a real button; clicking it toggles `#drawer-identity-edit` (hidden by default) open/closed, revealing a name input (`#drawer-name-input`, `OTR.updatePersonName`) above the color picker. Neither field had an edit path before this — the name was set once at the name gate and never touched again, and the color picker used to sit permanently expanded, reading as more "settings you must configure on arrival" than the identity block deserved. Collapsed by default now, and it collapses again on drawer close (`setIdentityEditOpen(false)` in `closeDrawer()`) so reopening the drawer later always starts from the current name, not whatever was left typed in. Name saves on blur (Enter just blurs — there's no surrounding `<form>` to submit). Hover on the avatar button uses the same `filter: brightness(1.15)` language as the header avatar trigger, since it's now interactive and the tint-hover trick doesn't read against a solid per-person fill. **Pencil icon** (`.drawer-name-btn`/`#drawer-name-btn`, Lucide `pencil`) — a second trigger sitting beside the name text, added because clicking the avatar was the *only* way to discover any of this was editable; the pencil makes "you can change this" visible at a glance instead of a hidden gesture. Two separate `<button>`s (name can't nest inside the avatar's own button) call the same shared `toggleIdentityEdit()`.
  - **Avatar color** (`.avatar-color-picker`, `AVATAR_COLORS` array in the inline script, `OTR.updatePersonColor` in `identity.js`, `people.avatar_color` column — see `supabase/migrations/2026-07-21-add-avatar-color.sql` and `2026-08-05-dark-mode-avatar-default.sql`): a row of eight swatch buttons, so people can tell each other's dots apart at a glance instead of everyone rendering as the same chip. Fixed palette, not a free color picker. Pink is deliberately excluded from the palette; it stays reserved for overlap/logo/hover (see `brand-brief.md`). "Paper" (near-white) used to be the automatic default for every new signup — meaning everyone looked identical until they thought to come pick a color themselves, which in practice most people never did. `randomAvatarColor()` now picks a random entry from the other seven — Rust, Amber, Berry, Jade, Sky, Indigo, Violet, each retuned to the logo's own saturation/lightness (54%/51%) and spread across hues that skip green and pink — at signup (`nameGateForm`'s submit handler, passed straight into `createPerson(name, randomAvatarColor())`), so a fresh person is distinguishable from the first show card they see. Paper stays in the palette as a manual pick, just not the default; existing people with a null `avatar_color` still fall back to it (no retroactive recolor). Picking a swatch writes straight to `people.avatar_color` and re-renders the drawer avatar, the header trigger, and (if a group is open) the current view. A `contrastTextFor()` helper computes each dot's text color (charcoal or white, by luminance) per-swatch rather than assuming white always reads.
    - **Moss -> Berry.** Moss (`#61c63f`) turned out to sit only ~20° away from `--color-logo-green` (`#8dc63f`) on the hue wheel — close enough to read as "part of the brand" rather than a distinct person, worse now that the header has its own green glow (see Header nav below). Contradicted the palette's own stated goal of skipping green/pink entirely — an oversight, not a deliberate choice. Replaced with Berry (`#c53fb4`), same 54%/51% saturation/lightness as every other swatch, hue picked to sit in the previously-empty gap between Violet (265°) and the pink accent (352°) — roughly 45° from each, and a genuinely more vibrant jewel tone than anything else in the lineup. Existing people with `avatar_color = '#61c63f'` keep that value (no retroactive recolor, same policy as Paper) — this only changes the picker and `randomAvatarColor()`'s pool going forward.
  - **Group switcher** (`.drawer-group-list`/`#drawer-group-list`/`#drawer-new-group-btn`, styles in `app/styles/components.css`) — plain always-visible rows now, not a dropdown. Went through two prior homes this session: started in the drawer, moved out to a dropdown under the header for more visibility, then briefly lived as a dropdown inside the header itself — and has now moved back into the drawer, this time deliberately, as a flat list rather than a popover. Reasoning for the flat-list format: once you're inside an opened drawer, a second click-to-open step on the group switcher is redundant friction, so it's just a plain clickable list — click a row to switch. Now the last section in the drawer (`.drawer-section-last`, no bottom border) since Crew was cut, below, sitting under **My Shows** (see its own entry further down). The section's own `.drawer-label` reads "Herds" now, not "Your groups" — per direct feedback, leaning into the app's own name/metaphor instead of the generic term. "Group" still appears elsewhere (name-gate's "Group name" field, "View group"/"Leave group" in the group modal) — not touched here, worth a consistency pass later.
    - **Row shape.** Went through three passes in the same session. First: every row (active or not) got a `--color-border` outline, since an inactive row with no visible edge at all read as broken. Second, per direct feedback ("remove the outline from the containers in the nav, they're not cool"): outline cut entirely, rows relying on padding + the `--space-8` gap between them alone. Third, once there were enough groups in the list to actually judge it ("we still need a container around the group even if it's not selected because if there are multiple it will get messy"): padding + gap on their own weren't enough definition with several rows — so every row now has a resting `--tint-hover` fill (not just on hover/active), with hover bumping to `--tint-active` and the active row sitting at that same `--tint-active` tone permanently (a deliberate reuse, not a third unique color) plus bolder text. Still no border anywhere, never the pink accent — just tonal fill doing the "this is a real row" job a line used to.
    - **"+ New group" — now matches Add Show's weight.** Went through two passes: first a bare text link with no shape at all, then a small tonal row matching the group rows' shape. Per direct follow-up feedback ("make New group in a container like Add Show"), it's a full `.add-show-card`-style treatment now — `--space-16` padding on all sides, a centered icon+label unit (`.drawer-new-group-icon`, same fixed-square-slot sizing as `.add-show-icon`), not a left-aligned row. Fill is `--tint-hover` (not `--color-surface`, which the drawer panel itself already uses — reusing it here would blend the button straight into the drawer background) — reads as a real card, not a quiet label, while still visually a step down from a group row, which is the actual primary content of this section. Label reads "Add a herd" — first tried as a bare "+ Herd" alongside the "Herds" section label above, then corrected per direct feedback ("plus herd is confusing") to match Add Show's own "+" icon + "Add a show" label pattern exactly, rather than a shorthand that read ambiguous next to the icon.
    - **Member count — mixed case.** `.group-switch-item-count` ("1 member") dropped its uppercase+tracked treatment, per direct feedback that it read as shouty for a plain fact sitting right under a name — small and dim, sentence case now.
    - **Per-group share button.** Each row now holds two independent controls, not one — select this group, or copy *its* invite link — so the row itself changed from a single `<button>` to a plain `.drawer-group-row` div wrapping two children: `.drawer-group-select` (the old button's content — name + count, `flex:1`, click → `selectGroup(g)`) and `.drawer-group-share` (a small circular icon button, Lucide `share`, `--space-32`). The card shape (border/radius/padding) moved from the old single button onto the row. `.drawer-group-share`'s click handler calls `e.stopPropagation()` before anything else — without it, a tap on "share" would bubble up and also fire `selectGroup()` on a row you weren't trying to switch to. Copies that specific group's link (its own `slug`, not necessarily the current group's) via the shared `copyInviteLink()` helper, then flashes the icon to a `check` for 1.5s (`flashShareSuccess()`) as feedback, since there's no text label on this button to swap the way the name-gate's "Copied!" does. Verified against fake data + a stubbed `lucide` (this sandbox can't reach the real Supabase project or unpkg CDN to test live): clicking a non-active row's share button copies that row's own slug and leaves the active row unchanged, confirming the stopPropagation actually holds.
  - **Crew — cut.** Used to be a plain name list for the current group's members below the group switcher. Removed: it was redundant with the per-show "who's in" avatar stacks and the group switcher's own "N members" subtitle, and it was the one section in the drawer that didn't do anything when tapped. `OTR.getGroupMembers` is unused in `index.html` now as a result (still exists in `identity.js` — a real API, just not called from the drawer anymore).
  - Closes via the close button (Lucide `x`, see "Icons — Lucide" above), clicking the overlay, or Escape. Body scroll locks while open.
  - Not yet built: switching which group is "current" doesn't persist across a reload (always defaults to the first group returned, or whichever group an invite link just added them to).
- **Add show button** (`#add-show-btn`, `.add-show-card`) — no longer a header-row button; it's the literal top item of the show list now, styled with the same surface fill/radius/padding as `.show-card`, sitting directly above `#show-list-content`. No outline or border at all — a dashed-border version was tried and cut for feeling too "define-me-as-a-control." Hover is just the label opacity brightening (0.7 → 1), the same quiet text-highlight language used elsewhere in the app. Toggles the inline panel open/closed in place (see "Add show card" below) rather than opening a modal. Hidden entirely on the My Shows tab (`renderCurrentView` sets `addShowCard.style.display` based on `activeTab`) — it's an action that only makes sense against the full group list.
- **Drawer footer** (`app/index.html`, `.drawer-footer-section`, styles in `app/styles/components.css`) — copyright line + contact link, the same two elements the old standalone page footer carried, moved into the nav drawer per direct feedback ("i want to move the footer content out to the drawer nav"). No longer a page-level element the whole document had to reserve space/scroll room for. **Locked to the drawer's own bottom edge** per direct follow-up feedback ("center and lock the footer content, 48px above the bottom") — no longer a normal-flow `.drawer-section`; `.drawer-footer-section` is `position: absolute` (`left`/`right: var(--space-24)`, `bottom: var(--space-48)`) inside `.nav-drawer`, which is both the positioned ancestor (`position: fixed`) and the scroll container (`overflow-y: auto`) — so it stays anchored to the drawer's own box rather than scrolling away with the herds list above it. Centered now (`align-items: center`, `text-align: center`) instead of the old left-aligned stack. `.nav-drawer` picked up extra `padding-bottom` (`calc(var(--space-48) * 2)`) so scrolled content never lands behind it.
  - **Copyright** (`.drawer-footer-copyright`): plain small text ("© 2026 GigHerd"), same quiet meta-line register `.drawer-label` already uses elsewhere in the drawer — no explicit color, inherits `--color-surface-text` from `.nav-drawer` itself.
  - **Contact** (`.drawer-footer-contact`): still a bare `mailto:` link, not a form — same reasoning as before (no email service/backend in this app's stack to receive a submission). Went through two copy passes: first "Questions? Comments? Email us.", then per direct follow-up feedback ("I think we show the email") the actual address is now visible text, not hidden behind generic link copy — a new `.drawer-footer-prompt` line ("Questions or comments?") sits above the link, and the link itself is the literal address. Address changed from the personal `dmurphy.dpm@gmail.com` placeholder to a real support alias, `hello@gigherd.com`. Same opacity-brightens-on-hover language as every other text-only control in the app (Add Show, decline) — no underline, no border.
  - **Old page-footer machinery removed with it.** `#site-footer`'s `siteHeader`/`mainContent` show/hide toggle lines in `init()`/`enterAppFromGate()` are gone (nothing to toggle — the drawer already handles its own open/close). The `body` flex-column + `.show-list`'s `flex: 1` in `grid.css`/`components.css` — originally added so the old footer could stay pinned to the viewport bottom on short pages — stayed in place rather than being ripped out: with no footer to pin, the same stretch now just keeps the page's own background filling the full viewport height on a short page (an empty group, a couple of shows) instead of stopping short. The now-orphaned `--color-footer-bg` token (`tokens.css`) was removed outright — zero remaining consumers once `.site-footer` itself was deleted.
- **Invite link — cut, then rebuilt, then folded into a real group modal.** The standalone "Invite a friend" button (main page, above the Add Show panel) was removed earlier this session; for a while afterward the only invite-link affordance left was the name-gate's one-time "Copy invite link" step, with no way to regenerate a link later. Went through two more shapes before landing here: first a share icon in the tab-bar row, then a labeled "Invite" button beside Add Show — both cut once it became clear a group needs more than one entry point (invite, see who's actually a member, leave), not just a link-copy shortcut. See **Group modal** below for where this settled. `.drawer-group-share` (the per-row icon in the drawer's group switcher, see above) is a separate, smaller affordance that's stayed put through all of this — it's about grabbing a link for a group you're not necessarily looking at right now, a different use case from the main-screen group modal, which is always about the *current* group. Domain/slug structure is still the placeholder `?g=<slug>` + `window.location.origin` — real-domain work is a separate, later step; nothing here changes that mechanism, just where it's triggered from.
  - **Copies a sentence, not a bare link.** `copyInviteLink(slug, groupName)` used to just write the raw URL to the clipboard — per direct feedback ("the text message for sharing needs clearer language, not just 'gig herd'"), a bare link with no surrounding words gives a recipient zero context if the messaging app it lands in doesn't render a rich preview (or if `og:*` tags aren't reachable yet on whatever domain this ends up on). Now copies `Join ${groupName} on GigHerd — line up shows with your pals: ${link}` — reuses the app's own existing tagline rather than writing separate marketing copy, and names the actual group rather than just the app in the abstract. `groupName` is a required second argument now; all three call sites (group heading row's share icon, group modal's invite button, each drawer group row's share icon) already had the group object in hand, so this was a one-line change at each. Voice matches `brand-brief.md`'s stated rules directly — no exclamation point, no "you're going to love this," nothing that reads as manufactured urgency.
- **Show card** (`app/index.html`, logic + `buildDateBadge`/`renderShowInteractiveBody` in the inline script, styles in `app/styles/components.css`) — vertical card: date badge, title row, venue (linked out), openers, two labeled avatar groups + ghost add. Built and wired to Supabase.
  - Surface: `--color-surface` fill — one step lighter than the (now charcoal) page — no border, `var(--radius)` (8px) corner radius. Anything inside the card that needs explicit text color uses `--color-surface-text` (warm paper).
  - **Date badge** (`.show-date-badge`, `buildDateBadge()`): plain text now, no pill at all. Went through two prior treatments — a clip-path "flag," then a ghosted pink chip — both cut; "ugly" was the direct call on the second one.
  - **Overlap badge — cut.** Used to be a filled pink `.show-overlap` chip ("N overlap") in `.show-card-top`, right-aligned next to the date badge, only rendered at 2+ curious. Removed per direct feedback — who's actually curious already shows up in the "Heck yeah!" avatar row below it, so the badge was a second, redundant count of the same number. `.show-card-top` is gone too (it existed solely to hold this badge — the date badge itself renders outside it, directly in the card). `overlapCount()` (`app/scripts/shows.js`) still exists and is still correct — it's just not called from anywhere in `index.html` anymore. No pink UI marker is left on a show card, or anywhere else in the app, now — the wordmark's "Gig" is the only remaining place `--color-accent` appears. Worth flagging: `01_Strategy/decisions.md` and `00_Brand/brand-brief.md` still describe overlap-surfacing as the app's core mechanic and pink as "the overlap color" — those weren't rewritten as part of this cut, since that's a brand-positioning question bigger than removing one UI element; revisit if the two ever need to be reconciled.
  - **Title row** (`.show-title-row`): just the headline text now. Used to carry a quiet `›` arrow opening a dedicated show detail page — that page and the arrow were both cut (not needed yet); the row wrapper stayed in place in case something else joins the title later, but today it holds nothing but the title.
  - **Actions — two labeled avatar groups**, replacing first a segmented Curious/I'm out control, then (briefly) a single avatar row + "Can't make it" text button: pulled research on Material Design 3, Apple HIG, and Airbnb's card patterns — Material explicitly warns against overloading a card with competing actions and says secondary actions belong in a corner; a segmented curious/out pair read as two equal-weight choices when the app's real point is a single "I'm in" tap. The button version worked but the "who's out" side stayed purely informational, not a real symmetric action. Now: `.show-card-avatars` holds two `.avatar-group` clusters side by side — "Heck yeah!" and "Can't make it" — separated by a real vertical rule (`.avatar-group-declined` carries `border-left` plus 16px of margin/padding on each side of it, splitting what used to be a plain 32px flex `gap` so the line sits centered in that same space) rather than space alone. Each group has its own small uppercase `.avatar-group-label`, a `buildAvatarDots` row of everyone in that group *except me*, and a fixed `.avatar-my-slot` that holds either `.avatar-ghost-add` (tap to join that specific list) or my own filled avatar dot, depending on whether that's already my status. `.avatar-group-row` carries the `gap` between the dots and `.avatar-my-slot` (rather than a `margin-left` on `.avatar-my-slot` itself) specifically so the label and the first avatar/ghost stay left-aligned even when the dots row is empty and the ghost is the only child. Built with one shared helper, `buildResponseGroup(label, peopleExceptMe, amIIn, joinAriaLabel, onJoin, allPeople)`, called once per group so both stay in sync structurally. Whichever group isn't my current status shows its ghost, so I can always switch between them. `.avatar-group-declined`'s label runs a bit dimmer than the "Heck yeah!" one — the only remaining visual hint that it's the secondary group — but the avatars/ghost inside it stay full-strength, not dimmed like the earlier info-only version, since tapping into that group is now a real action. Tapping either ghost calls `setResponse(..., 'curious' | 'out')` directly (`e.stopPropagation()` on the ghost's own click handler, so joining a group doesn't also trigger the group's people-modal below), no intermediate button state. Deliberately not pink on hover (fills the paper chip color instead, matching the app's existing "active chip" language) — pink stays overlap-only.
  - **Who's-in-this-group modal**: clicking anywhere on either `.avatar-group` (not just an avatar) calls `openPeopleModal(label, allPeople)` — a shared modal (`#people-modal-overlay`/`.modal-box`) listing everyone in that specific group, one row per person (`.person-list-item`: avatar dot + name), with the group's own label as the modal heading and a close button (Lucide `x`). Closes via the close button, clicking the overlay, or Escape. Reintroduced the modal pattern here deliberately, after cutting it for Add Show — this is a genuinely focused, occasional "who's in" lookup rather than a frequent primary action, so a modal still earns its keep even on a page whose overlap dimming/focus-stealing was ruled out for Add Show.
  - Renders inside: a group's show list (built), and the cross-group My Shows view (see below) — "Most overlap" sort not yet built, both sort by `show_date` ascending.
  - **Empty state** (`.show-list-empty`, `renderEmptyState()` helper, shared by "no shows in this tab" and "no group yet"): centered (icon + message stacked, not left-aligned body text) with a small flat filled-shape guitar silhouette above the message — headstock, neck, a figure-8 body built from two overlapping filled circles, and a soundhole punched through with `--color-bg`. A stroke-based spotlight icon was tried first and cut ("not working") — filled shapes read more cleanly than a spotlight's crossing stroke outlines would have. Same helper renders all three empty messages ("No shows yet.", "You haven't marked any shows yet.", "Create a group to start adding shows.") so the icon+centering treatment can't drift out of sync between them.
    - **Text/icon color — unified.** The message text used to inherit `--color-text` at full opacity while the icon sat at 0.35 opacity below it, so the two read as two different tones of the same idea instead of one quiet voice. The 0.35 opacity moved up onto the shared `.show-list-empty` container itself — both the message and the icon (an inline SVG using `fill="currentColor"`) now pick up the exact same dimmed tone.
    - **Icon size — doubled.** `.show-list-empty-icon` went from `--space-56` to `calc(var(--space-56) * 2)` (112px), per direct feedback that the guitar read too small/timid against how much empty space surrounds it. `calc()`'d off the existing token rather than a new hardcoded px value.
    - **Big darker circle backdrop.** Per direct feedback ("let's do a big darker circle bg behind the empty state icon and text"), the icon and message now sit inside a new `.show-list-empty-circle` wrapper — a `--color-fill-dim` circle, same token/philosophy as the drawer identity band and footer above. The circle itself deliberately does *not* inherit the 0.35 dim the icon/message carry — a circle at 35% opacity against the already-dark page would read as barely-there, not "a big darker circle" — so the opacity stayed scoped to just the icon and message elements inside it, while the circle's own fill stays at full strength. `renderEmptyState()` now wraps its markup in `.show-list-empty-circle`; same one helper still renders all three empty messages. Sized at 280px (bumped up from an initial 220px per direct follow-up feedback that it still needed to be bigger), `max-width: 100%` still caps it on narrow screens.
  - **Loading state** (`.show-list-loading`, static markup in `index.html`, not a JS-rendered helper): a hand-authored inline SVG spinner (`.show-list-loading-icon`, a partial-arc `<circle>` with `stroke-dasharray`) that rotates via a plain `@keyframes` CSS animation, replacing the old bare "Loading…" text — per direct feedback ("can we animate an icon or something?"). Deliberately its own class rather than reusing `.show-list-empty` — a real "no shows" state and a brief initial-fetch state shouldn't share styling just because both are momentarily contentless, and this markup always gets overwritten by `renderShows()`/`renderEmptyState()` once data actually comes back, so it never needs the empty state's own message/circle-backdrop treatment. Same dimmed `currentColor`-on-`--color-text` language as the empty-state icon (0.35 opacity) so it doesn't compete for attention. Hand-authored rather than a Lucide icon reference on purpose — a CDN icon load can fail silently with no visible error (the exact bug that hit the favicon font earlier this project), so a dependency-free inline SVG matches the same reasoning already behind the empty-state guitar icon. Respects `prefers-reduced-motion: reduce` (animation disabled outright) — the spin is decoration, not information.
    - **Copy — consolidated to one template.** The three empty messages used to be differently structured sentences ("Create a group to start adding shows.", "No shows yet.", "You haven't marked any shows yet — across any of your groups."). All three are now "No ___ yet." — "No groups yet.", "No shows yet.", "No shows marked yet." — one consistent, simpler pattern instead of three ad hoc phrasings.
  - **Card state:** when the current person's own response is "out," the whole card drops to 50% opacity (`.is-out`) rather than styling anything inside it differently — the dimming itself is the "you've deprioritized this" signal. My own status inside the card shows up as my filled dot sitting in the "Can't make it" group instead of a button label change, now that there's no standalone decline button left.
  - Overlap badge: filled block (accent bg, not just colored text) so it reads as a marker, not a label. Only shows when 2+ people are curious (`overlapCount` — out never counts).
  - **Spacing:** the text block (title row, venue, openers) and the avatar row below it get a bit more room to breathe than earlier passes — `.show-venue`/`.show-openers` each carry `margin-bottom`, and `.show-card-avatars` carries `margin-top: var(--space-16)`; adjacent margins collapse to the larger value per normal CSS block flow, so the visible gap ends up 16px rather than a cramped 8px.
  - Who's-in avatar dots (`buildAvatarDots(people)`): plain filled dots, no click behavior of their own — clicking anywhere in a group opens the shared people-modal described above instead. Each dot is filled with that person's own chosen avatar color (see "Avatar color" under Nav drawer) instead of a uniform charcoal — the identity signal the color picker exists for.
  - Not yet built: editing/deleting a show.
- **Add show card** (`app/index.html`, `#add-show-card`/`#add-show-btn`/`#add-show-panel`, logic in `app/scripts/shows.js`, Edge Functions in `app/supabase/functions/`) — one card, always, same `.show-card`-style surface and full width as the rest of the list. Went through three prior treatments: a centered modal first (flat dimming overlay, cut — adding a show is a quick, frequent action, not a focused task that earns dimming the page), then a version with a separate small `.modal-close-btn` "×" inside the panel's own title row (cut — a full-size CTA collapsing to a tiny corner icon read as two different controls), then a version where the toggle button and the form panel were visually two separate cards stacked (cut — opening it looked like a second box appearing, not the same card responding). `#add-show-card` uses a single constant `padding: var(--space-16)`, closed or open — a shorter `--space-8` closed-state padding was tried and reverted twice over: it read as too short a row, and toggling between two different padding values (with no transition on padding itself) made the card visibly *jump* in height a beat before the panel's own smooth grid-rows transition caught up. One constant padding value fixed both — taller resting row, and no more jump, since padding no longer changes at all between states; only the panel's height animates. `#add-show-btn` (`.add-show-toggle`) is a plain-background row inside it — no idle-state dimming, full color always. Holds `.add-show-icon` ("+") and `.add-show-label` ("Add a show") — the label text itself never changes between states; only the icon communicates open/closed.
  - **Label size — revised down.** Originally set to the exact same token set as `.show-title` (`--text-sub-size`/`--text-sub-lh`, 20px) on the reasoning that "it's the first thing on the page and should carry the same presence as every card title below it." Dropped to `--text-control-size`/`--text-control-lh` (14px) once Add Show had to share its row with the group menu button beside it (see below) — at 20px next to a smaller companion button it read oversized, more like a headline than a button label. `--text-control-size` is the type scale's own named token for "buttons, links, tappable controls" (see the type table under Layout & Grid), so this also resolves a small inconsistency: Add Show's label had been the one button-like control in the app *not* using that token. `.add-show-icon`'s box shrank to match (`--text-control-lh` instead of `--text-sub-lh`), though the "+" glyph itself stays at `--text-sub-size` inside that smaller box — a bit of deliberate oversizing so the glyph doesn't shrink into looking spindly.
  - **Row layout — briefly shared, now full width again.** For a couple of passes `#add-show-card` sat inside a `.show-list-actions` flex row alongside a group-menu button, `flex: 1` so it still took up most of the width. Reverted per direct feedback: the group action moved out to its own heading row above the tabs (see **Group heading row + group modal** below), so `#add-show-card` is back to being the sole, full-width first item in the list, its own `margin-bottom` back on the card itself.

  **Horizontal centering — tried optical-perfect, reverted to grouped.** The first pass centered icon+label as one flex group (`justify-content: center`, `gap` between two children). That kept reading as visually off-center even after a square-icon-box pass and an `appearance: none` button-reset pass (both real, worthwhile fixes — see "Base reset" above — but neither was the actual cause). Rendering the button headless and reading real `getBoundingClientRect()`/text-ink positions proved the flex version's math was already exact — the group's true geometric center landed within a fraction of a px of the button's center, every time. The mismatch was optical: a thin "+" glyph carries far less visual weight than the dense label text next to it, so centering their *combined bounding box* left the visually heavier label sitting right of where the eye expects center. The fix at the time was a 3-column grid (`var(--text-sub-lh) 1fr var(--text-sub-lh)`) that centered only the label, with the icon pinned in its own column on the left and a mirrored empty column on the right — which did land the label on the button's exact center, re-verified the same way. But it also visibly separated the icon from the label, and per direct feedback, reading them as one grouped, adjacent unit mattered more than perfect optical centering. Reverted to the original flex + `gap` — icon and label sit together again (8px apart), centered as a pair; the small optical drift this reintroduces was an accepted tradeoff, not an oversight.

  **Vertical centering — a separate, real bug, not an optical one.** "Add a show" was floating noticeably above the closed card's true center. Headless measurement caught it precisely: `#add-show-panel`, even fully closed, wasn't collapsing to 0 height — it was stuck at exactly 16px, the value of `.add-show-panel-inner`'s own `padding-top`. The cause: `overflow: hidden` only zeroes out a *child's* min-content contribution to grid track sizing — it does nothing to the box's *own* padding, which always counts toward its size regardless of overflow. `.add-show-panel-inner` had both `overflow: hidden` and `padding-top: var(--space-16)` on the same element, so the "collapse to 0" trick (documented above) was quietly capped at 16px instead of reaching 0 the whole time. That extra 16px sat below the button inside the card (8px real top padding vs. 8px + 16px leftover below), which is exactly what made the button read as floating high. Fix: the padding moved one level deeper, onto a new `.add-show-panel-content` wrapper *inside* `.add-show-panel-inner` — still renders identically once open, but no longer prevents the boundary itself from reaching true 0 while closed. Re-verified headless: space above the button now exactly equals space below it (8px/8px) in the closed state.

  `#add-show-panel` is the form content, laid out with `display: grid; grid-template-rows: 0fr` that transitions to `1fr` on `.is-open` (a CSS grid-rows accordion — animates smoothly to the content's natural height with no px guessing, the one gotcha being `#add-show-panel-inner` needs its own `overflow: hidden` for the collapse to reach true 0). Clicking `#add-show-btn` toggles `.is-open` on the outer card, which drives the padding change above plus the icon's 45° rotation (a `+` rotated 45° reads as an `×`, one glyph covers both states) and the panel's grow/shrink, all at once — same button opens and closes, no separate close control. Escape and the existing Cancel button (inside the manual form) also close it. Hidden entirely on the My Shows tab (see "Add show button" above). Two entry methods inside — "paste a link" was cut (see below); search results submit directly, so the review form only appears for plain manual entry. Manual fields are trimmed to title (required), date (required), and venue name — no time, no visible venue-link field, no openers field.
  - **Method 1 — artist search** (default/primary): `OTR.searchShows(keyword)` calls the `search-tickets` Edge Function, which queries Ticketmaster's Discovery API across New England (MA/CT/RI/NH/VT/ME — six requests, one per state, rather than gambling on undocumented multi-state syntax) and returns a clickable results list. Clicking a result calls `submitShowDirectly` — creates the show immediately, no form shown.
  - **Method 2 — add manually**: skips straight to the blank form.
  - **"Paste a link" — cut.** A third method (`OTR.parseShowLink(url)`, the `parse-show-link` Edge Function) fetched a pasted URL server-side and parsed JSON-LD `Event`/Open Graph data. Removed as unneeded alongside this batch's other simplifications. Known limitation while it existed, worth remembering if it comes back: Ticketmaster and AXS both blocked it outright — it only ever worked well against venue sites without bot protection. `parse-show-link` itself, `formatPriceRange()`, and the `#show-link-method-btn`/`#link-method-row` UI were all removed with it; the Edge Function file is still on disk but unused.
  - **Venue autocomplete**: typing 3+ characters into the Venue field debounces a call to `OTR.searchVenues(keyword)` (new `search-venues` Edge Function, same Ticketmaster-venues-by-New-England-state pattern as search-tickets, reuses the same `TICKETMASTER_API_KEY` secret). Picking a suggestion sets the venue name and silently stores its venue link in a script-scoped `selectedVenueUrl` variable — there's no visible link field for it to appear in; it just rides along into `createShow`.
  - Coverage tradeoff, worth remembering: Ticketmaster/Live Nation controls ~80% of *major* concert venues' primary ticketing, but that skews toward arenas/amphitheaters — small clubs and dive bars (the venues this app is actually about) are much less likely to be covered by either method, which is why manual entry stays a first-class path, not an apology.
  - Plain bordered inputs, no styling framework — `.form-row`/`.btn-solid`/`.btn-outline`/`.inline-input-row`/`.link-btn`/`.search-results` are reusable primitives in `components.css` now, not one-offs.
  - Not yet built: editing/deleting a show; joining an *existing* group via invite link (mirrors the header nav gap).
- **Group heading row + group modal** (`.group-heading-row`, `#group-modal-overlay`, `app/index.html`, styles in `app/styles/components.css`) — page context (which group is this) plus the entry point to managing it. Went through two prior shapes: first a plain "Invite" button beside Add Show, then a two-line button (group name + "View members" subtitle) that opened the modal directly — both cut. The button version put "which group am I in" and "Add a show" fighting for the same row, and per direct feedback, Add Show needed to be the *only* thing there. Landed here: a real heading (`<h2 class="group-heading-name">`, not a button) naming the current group, sitting in its own row between the header/drawer and the tab bar, with two small icon-only circular actions (`.group-heading-btn`, same shape as `.drawer-close-btn`) on the right — Share and View.
  - **Share** (`#group-share-btn`, Lucide `share`) — copies the current group's invite link directly via `copyInviteLink()`, one tap, no modal in the way. Icon flashes to a `check` on success (`flashShareSuccess()`) — same pattern as the drawer's per-group share icons, since there's no text label here to swap.
  - **View** (`#group-view-btn`, Lucide `users`) — opens the group modal.
  - **Group modal** (`.modal-overlay`/`.modal-box`, same primitives as the show-card's people-modal, just a second instance with its own content): three things in order, deliberately weighted by how often each gets used.
    1. **Invite people** (`.group-modal-invite-btn`, `.btn-solid` — the strongest visual treatment in the modal) — copies the invite link the same way the Share icon does. Redundant with it, on purpose: someone who opened the modal to check the member list shouldn't have to close it and hunt for Share separately if they decide to invite someone while they're in there. Has a real text label, so success is a text swap to "Copied!" (matches the name-gate's own invite button).
    2. **Member list** (`.group-modal-label` "Members" + `#group-modal-member-list`) — the actual roster of the group, via `OTR.getGroupMembers(groupId)` (an existing function that had gone unused since the drawer's Crew section was cut — now has a real caller again). Different from the show-card's people-modal, which only lists who's responded to a *specific show*; this is everyone in the group regardless of any show. Both reuse the same row-building logic now (`renderPersonList()`, factored out of what used to be `openPeopleModal`'s own inline DOM-building, so the group modal didn't need a second near-identical implementation).
    3. **Leave group** (`.group-modal-leave-btn`) — deliberately the quietest thing in the modal: plain centered text, muted opacity. Separated from the member list above by space alone now (`margin-top: var(--space-32)`), not a `border-top` — per direct feedback ("I don't love the stroke above leave group, let's just use a little space instead"), same "separation through room, not a line" call already made for the drawer's identity section and footer. No filled or red destructive-button treatment — the app has no destructive-red token anywhere else, and one rare action didn't seem worth introducing one for. Confirms first via a plain `window.confirm()` (the same "reach for the browser's own primitive before building custom chrome" approach already used for the clipboard-copy fallback), then calls `OTR.leaveGroup(personId, groupId)` (deletes the `memberships` row — the direct inverse of `joinGroup`), removes the group from `myGroups`, closes the modal, and either lands on the next remaining group (`selectGroup(myGroups[0])`, which also re-renders the drawer's group switcher) or falls back to the same zero-groups empty state a brand-new person sees.
  - **New herd modal** (`#new-group-modal-overlay`, `#new-group-form`) — replaces what used to be a bare `window.prompt('Name this group:', '')`, per direct feedback that a native browser dialog "feels weird," looks inconsistent across Safari/Chrome, and (unlike every other modal in the app) has no scrim behind it. Same `.modal-overlay`/`.modal-box` primitives as the group and people modals, with a single `.form-row` text input (same filled-field treatment as the name-gate's own "Group name" field) and a full-width `.btn-solid` submit. Opened from the drawer's "+ Herd" button (`handleNewGroup()` now just calls `openNewGroupModal()`); the actual `createGroup()`/`trackEvent()`/`selectGroup()` call sequence that used to live in `handleNewGroup()` moved into the form's `submit` handler. Closes via the close button, overlay click, or Escape — same conventions as the other two modals.
  - **Visibility differs from Add Show's on purpose.** `.group-heading-row` is set inside `selectGroup()` and stays visible on *both* tabs (`renderCurrentView` no longer touches it) — knowing which group you're looking at isn't a Herd-Picks-only concern the way adding a show is. Hidden entirely in the zero-groups state, same as before.
  - **Centered, actions stacked below.** Was name-left/actions-right in one row; per direct feedback, `.group-heading-row` is a centered column now — the heading on top, `.group-heading-actions` as its own centered row underneath rather than crowding the name on one line. `#my-shows-heading-row` reuses the same `.group-heading-row` class (see its own entry above), so this centering applies there too, even though it has no actions row of its own.
  - **Action icons — ghost circles.** `.group-heading-btn` (share, view) is a soft translucent-fill circle at rest (`background: var(--tint-hover)`, no border), solid paper fill on hover — same language `.avatar-ghost-add` uses on the show cards below. Both used to be dashed-border outlines instead; per direct feedback ("i dont like the dotted lines"), both moved to this filled-circle treatment together rather than one getting fixed and the other left inconsistent.
  - Not yet built: renaming a group, removing a *different* member (only leaving yourself is possible right now), any confirmation beyond the one `window.confirm()` if leaving the current group leaves you with zero groups left.
- **Herd Picks / My Shows tab bar — cut.** Used to sit above the group list, splitting it into the full group list ("Herd Picks") vs. a client-side filter to shows this person personally marked curious ("My Shows"), scoped to whichever group was currently selected. Removed per direct feedback once cross-group My Shows (below) existed — the per-group tab could only ever answer "what have I marked *in this group*," and once that same question has a real cross-group answer, keeping a scoped, weaker version of it around wasn't earning its spot as a second control. A group's page is back to being just the one full list, unfiltered, same as before tabs existed at all. `activeTab`, `tabBar`, `.tab-bar`/`.tab-btn`, and the tab click handler are gone from `index.html`/`components.css` entirely, not just hidden. Add Show is unconditionally visible on a group's page again — it used to hide specifically on the My Shows tab.
- **My Shows (cross-group)** (`.drawer-my-shows-btn`/`#drawer-my-shows-btn`, `#my-shows-heading-row`, `renderMyShowsView()` in `index.html`) — reached from the nav drawer, not a tab, and isn't scoped to any one group: every show, across every group this person belongs to, that they've personally marked curious. This is now the *only* "My Shows" in the app (see the cut entry above) — it started life alongside the per-group tab as a second, broader way to ask the same question, and ended up replacing it outright. Sits inside `#group-bar`, above the "Your groups" list, in its own container button (same tonal shape as a group row) rather than one more row in the list, since it isn't a group.
  - **Data.** No dedicated cross-group query — `renderMyShowsView()` calls the existing single-group `loadGroupData()` once per entry in `myGroups` (`Promise.all`, in parallel), filters each group's shows down to ones this person marked curious, tags each surviving show with `_groupName` (the group it came from), flattens everything into one list, and sorts by `show_date`. `myGroups` is already loaded client-side by the time this can be reached, so this is a handful of parallel requests, not a new backend endpoint.
  - **Rendering.** Reuses `renderListView()` — the same show-card builder the group view uses. `renderListView()` has an optional 4th `onChange` argument for this: the group view's own call still defaults to refreshing the current group (`renderCurrentView(currentGroupId)`) after a response changes, but this view passes its own `() => renderMyShowsView()` so toggling curious/out on a card here re-runs the aggregate fetch instead of trying to refresh a group that isn't selected.
  - **`.show-group-tag`** — a small dim meta line under the date badge, only rendered when a card carries `_groupName`. A group's own page never sets it (no ambiguity there, the group heading row above the list already says which group you're in); here it's the one place a card needs to say which group it belongs to, since the list spans several.
  - **Heading.** `#my-shows-heading-row` reuses `.group-heading-row`'s h2 styling (same visual weight as "which group am I in") but with no share/view action buttons — there's no single group here to act on. `group-heading-row` and Add Show both hide while this view is active (`currentGroupId`/`currentGroup` set to `null`); Add Show specifically because there's no single group to add a show *to* from this view.
  - Not yet built: filtering the aggregate list back down to one group from within this view; a "declined" (out) equivalent — only "curious" rolls up here.
  - **Chevron + copy + case.** A trailing Lucide `chevron-right` marks the button as a tappable destination — a group row implies the same thing through its share icon and two-line content, but this button had nothing else suggesting "tap me" beyond the tonal fill. `.drawer-my-shows-sub` dropped its uppercase+tracked treatment (mixed case now, same reasoning as the member-count fix above) and the copy itself changed from "Everything you're in, across groups" (read awkwardly as a sentence) to "Everything you've marked, across your groups." — also picked up an explicit `line-height: var(--text-meta-lh)` once the new (longer) copy started wrapping to two lines and visibly inheriting body copy's much taller default line-height, which read as an odd, uneven gap.
    - **Chevron circle.** First pass just sized the bare svg (`--icon-size`, centered via the button's own `align-items: center`) — read as misaligned/off-weight next to `.drawer-group-share`'s icon, per direct feedback ("maybe it needs a circle around it ... the share icon does, you can see it on hover"). Wrapped in `.drawer-my-shows-arrow-circle`, the same `--space-32` circle `.drawer-group-share` uses, so the two have identical footprints.
    - **Row hover — was effectively invisible.** The two-tier hover this circle originally had (row washes to `--tint-active` on hover, circle bumps further to `--tint-active-strong` so it stays visibly distinct on top) had a real bug hiding in it: `--tint-hover` (the row's resting fill) and `--tint-active` (its hover fill) are 8% and 10% white overlays respectively — only 2 percentage points apart, functionally the same color. Hovering anywhere on the row *except* the circle read as nothing happening at all, while the circle's jump to 30% looked like the only thing responding — exactly the bug reported ("hover on my shows should highlight the whole row not just the arrow icon"). Fixed by moving the row's own `:hover` straight to `--tint-active-strong`, the same strong tint the circle used to reserve for itself — genuinely visible now on the whole button, not just the corner. The circle's own separate hover rule became redundant once the row matched it exactly (same color, no contrast left to preserve) and was removed rather than left as dead-equal CSS.
  - **Selected state.** `drawerMyShowsBtn.classList` picks up `.active` (same `--tint-active` fill a selected group row gets, no border, never pink) whenever this view is showing, and loses it the moment a group is selected instead — set directly in `renderMyShowsView()`/`selectGroup()`. `renderMyShowsView()` also now calls `renderGroupSwitcher(myGroups)` (previously only `selectGroup()` did), so every group row's own `.active` class clears the instant you're looking at My Shows instead of the last-selected group still showing highlighted underneath. Added per direct feedback — reopening the drawer used to give no indication of which section you were actually looking at.
  - **Decline — avatar moves + inline Undo, not a frozen card behind a fade.** Marking a show "out" here removes it from the very list it's sitting in, since My Shows only ever shows curious picks. That used to just vanish the card the instant you tapped — jarring, and no way back from a misclick — then went through an intermediate pass (fade + a separate "Marked can't make it." bar below the card) that still left the avatar itself frozen in its old "Heck yeah!" spot while the surrounding content faded, no real visual confirmation the status had actually changed. Per direct feedback ("the avatar should move to that side of the list ... don't say marked can't make it, just add the undo button next to the avatar"), this is rebuilt around the avatar actually moving:
    - `renderShowInteractiveBody()` now returns the "Can't make it" group's own DOM node (`cantMakeItGroup`) so callers can reach into it after a render, and takes the same optional 5th `onDeclineOverride` argument it always has.
    - `renderListView()`'s `declineWithUndo` flag (only set `true` by `renderMyShowsView()`) builds one via `handleDeclineWithUndo(show, card, body, responses, refresh)` per card — the card and the show's live `responses` array are now passed in alongside the body, not just the body.
    - Clicking decline calls `setResponse(..., 'out')` immediately (the data change is real right away, not deferred), then locally patches a copy of `responses` to reflect the new `'out'` status and re-invokes `renderShowInteractiveBody(body, show, updatedResponses, refresh)` — same instant-reflect approach every other response click in the app already uses (no virtual DOM/FLIP animation anywhere in this codebase; a full rebuild is what "moving" means here). The avatar lands in the "Can't make it" slot on that very re-render, with a ghost "+" appearing back in "Heck yeah!" in its place — real confirmation the status changed, not a static avatar sitting behind a dimming card.
    - The returned `declinedGroup` node is used to find `.avatar-my-slot` (`declinedGroup.querySelector('.avatar-my-slot')`) and drop a plain `.show-undo-btn` (`.link-btn`) directly inside it, right next to the now-moved avatar dot — `.avatar-my-slot` picked up `display: flex; align-items: center` for this (it used to rely on default block stacking, which was fine when it only ever held one child, but put the Undo button on its own line underneath the avatar instead of beside it). The old separate `.show-undo-bar` element and its "Marked can't make it." text are gone entirely — the moved avatar now carries that meaning on its own, no caption needed. `e.stopPropagation()` on the Undo button's own click, same reasoning as the ghost-add buttons — otherwise the click would bubble up to the group's own click handler and open the people-modal instead of undoing.
    - The card itself still dims slowly in the background as a "this is pending removal" signal (`.show-card-content-fading`, applied to `card` now instead of `body`), but the opacity floor and duration were both softened further — 0.8s still read as "too sudden" per direct follow-up feedback, so it's now 1.4s at a lighter 0.55 opacity floor (was 0.4) — a slower, gentler settle rather than a snap.
    - A 4-second timer (`UNDO_WINDOW_MS`, unchanged) still calls the same `refresh()` every other path uses if Undo isn't clicked, which re-runs the aggregate fetch and the card is genuinely gone (now `'out'`, filtered out by the curious-only query). Clicking Undo before the timer fires cancels it, flips the response back to `'curious'` via `setResponse`, removes `.show-card-content-fading`, and re-renders the body a third time with the restored responses — the avatar moves back to "Heck yeah!" the same way it moved out, no separate cleanup path for the undo case.
- **Show detail page — cut.** A dedicated per-show page (title-row arrow → "‹ Back") used to hold price/"Buy tickets" (`formatPriceRange()`, Ticketmaster `priceRanges`/JSON-LD `offers`) and a "Listen on Spotify" link (`spotify_url`, sourced free from Ticketmaster's attraction data — a real Spotify Web API integration was checked and ruled out given their February 2026 Developer Mode lockdown). Removed entirely, not needed yet: `showDetailView`/`showDetailBackBtn`/`showDetailCard`, `openShowDetail()`/`renderShowDetail()`/`closeShowDetail()`, and `formatPriceRange()` are all gone from `index.html`. `renderShowInteractiveBody()` still exists and is now list-card-only (its signature dropped the `onViewDetails` callback along with the arrow). Price, "Buy tickets," and "Listen on Spotify" have no UI anywhere right now — revisit if that data turns out to be worth surfacing again, possibly back on the card itself rather than a separate page.
- Avatar dots (`buildAvatarDots`) — plain filled dots, no independent click behavior. Used for every show card's who's-in/who's-out rows; clicking the surrounding `.avatar-group` (not an individual dot) opens the shared people-modal (`openPeopleModal`) described under "Show card" above. The old per-header "crew" click-to-expand list this once mirrored was cut along with the drawer's Crew section (see Nav drawer above).
- **Favicon + link-share preview** (`app/assets/*`, `<head>` in `app/index.html`) — the app had neither before this pass; a plain browser-default page icon and a blank link preview when shared. Both generated from the same two source treatments (rendered headless, not hand-drawn) so they can't drift from the actual brand:
  - **Favicon** — a tight "GH" monogram, Big Shoulders Display 900 (same weight as the wordmark), pink "G"/green "H" on the app's own charcoal (`--color-bg`). One 512px master exported to `favicon.ico` (16/32/48px, broadest legacy support) plus standalone PNGs — `apple-touch-icon.png` (180px, iOS home-screen bookmarks) and `icon-192`/`icon-512` (Android/PWA "add to home screen"). Five `<link rel="icon">`/`<link rel="apple-touch-icon">` tags in `<head>` cover all of them.
  - **OG/Twitter card** (`assets/og-image.png`, 1200×630) — the wordmark + tagline ("Line up shows with your pals.") centered on charcoal, the same brand treatment as the header/name-gate, not a UI screenshot. `og:title`/`og:description`/`og:image`/`twitter:*` meta tags added; `og:url` deliberately left out rather than hardcoding a domain — there isn't a real one yet (see the Invite link entry above, still `window.location.origin` + `?g=<slug>`). Image `<meta>` paths are relative, which resolves fine same-origin once this is actually deployed somewhere.
  - **Font bug, caught and fixed.** The first pass pointed both master HTML files at Google Fonts the same way `index.html` does (`<link href="fonts.googleapis.com/...">`), same as every other headless render this session. That silently failed — this sandbox's network can reach `registry.npmjs.org`/`pypi.org` but not `fonts.googleapis.com`/`fonts.gstatic.com`, and a failed web font load has no error, it just falls back to the browser's default sans — so the first favicon/OG image round shipped in a generic bold system font, not Big Shoulders Display, without that being visually obvious in isolation (per direct feedback catching it: "how come we're not using the correct font ... it should match what we use in the header nav"). Fixed by pulling the actual font files a different way — the `@fontsource/big-shoulders-display` and `@fontsource/public-sans` npm packages (registry.npmjs.org, which *is* reachable) ship the real `.woff2` files directly, no Google Fonts network call needed — referenced via a local `@font-face` in the two master HTML files instead of a `<link>` tag. Re-rendered and regenerated every exported size from the corrected masters; the "G"/"H" now reads with Big Shoulders Display's actual condensed, high-x-height letterforms even at 32px, not a generic bold substitute.
  - **OG wordmark — all caps.** Per direct follow-up feedback, `og-image.png`'s wordmark is `text-transform: uppercase` ("GIGHERD") — the header/name-gate wordmark elsewhere in the app is untouched, this is scoped to the share-image master only.
- **Search/share meta + web manifest** (`<head>` in `app/index.html`, `app/site.webmanifest`) — the app had a bare `<title>` and nothing else; no description, no theme color, no manifest, so a plain link/search result had nothing to show beyond the filename-derived title. Added `<meta name="description">` (same shared tagline as everywhere else — "Line up shows with your pals.", not separate marketing copy), `<meta name="theme-color">` (`--color-bg`, colors mobile browser chrome/task-switcher to match the app instead of defaulting to white), `og:site_name`, and `site.webmanifest` (name/short_name "GigHerd", `--color-bg` for both `background_color`/`theme_color`, pointed at the same `icon-192`/`icon-512` files the favicon set already generated — one shared icon set, not a second manifest-only pair) referenced via `<link rel="manifest">`. Also caught in the same pass: `<title>`/`og:title`/`twitter:title`, and the footer's plain-text copyright line, all read "Gig Herd" with a space — inconsistent with the actual wordmark (`<span>Gig</span><span>Herd</span>`, no space between the two spans). Corrected everywhere to "GigHerd," one spelling of the name across the whole app, not just the colored logo treatment.

## Data Flow
Real data end to end now. `renderShowList(groupId)` fetches shows for the current group, then all responses for those shows in one query (`getResponsesForShows`), grouped client-side by `show_id`. Clicking a response button writes to Supabase and re-fetches the whole list rather than patching state locally — simplest thing that works at this scale, revisit if the group's show count grows enough that a full re-fetch per click feels slow.

## State
- Active tab (Herd Picks / My Shows): local state (`activeTab`), built — no "Most overlap" sort yet on either tab
- Show list + per-person responses: real data from Supabase, re-fetched on every response click (built)
- Expanded/collapsed avatar lists and group menu: local state, per-instance (built, in header nav)
- Person identity: `otr_person_id` cookie, backed by a real `people` row in Supabase (built)
- Current group, list of joined groups: real data from Supabase (`memberships` joined to `groups`/`people`) — no more mock data
- Open question resolved: multiple non-overlapping friend groups are supported — a person can belong to more than one group, switcher lists all of them.
- Still open: where the board lives (standalone vs. bot-in-chat), invite-link join flow, adding shows through the UI (Table Editor only for now), replacing the placeholder `prompt()` name entry with real onboarding UI.
