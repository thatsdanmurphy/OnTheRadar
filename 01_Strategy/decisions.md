# Decision Log

## 2026-07-16 — Core mechanic is overlap surfacing, not artist tracking
**What:** The product's central feature is detecting when 2+ friends independently mark curiosity about the same show, and surfacing that automatically.
**Why:** Existing tools (Spotify's unbuilt "gig buddies" request, Bandsintown's friend-attendance layer, stranger-matching apps like EventBuddie/Muse) either don't build this or solve a different problem (companion-finding, not friend-group overlap). The gap is specifically "surface overlap within a group I already know."
**Revisit if:** Usage data shows people care more about tracking individual artists than seeing overlap — would suggest the product should pivot toward artist-tracking-first with overlap as a secondary layer.

## 2026-07-16 — Response states are three-way: curious / going / out
**What:** Each person can mark a show as "I'm curious," "Got tickets," or "I'm out" — only curious + going count toward the overlap indicator.
**Why:** A binary interested/not-interested loses the distinction between "I'd consider it" and "I already committed." Excluding "I'm out" from the overlap count keeps the signal meaningful — overlap should mean real shared interest, not just visibility.
**Revisit if:** Users find three states confusing or the "I'm out" state goes mostly unused — could simplify back to two states.
**Superseded by:** the 2026-07-17 entry below — cut back down to two states, curious/out.

## 2026-07-16 — Visual identity is deliberately flat (ticket-stub / marquee)
**What:** No gradients, no glow/pulse animation, thin shadows only. Typography and color built around a printed-ticket-stub metaphor.
**Why:** Established in the working prototype (`on-the-radar.html`) and validated by feel — a concert board should feel like a corkboard of stubs, not a SaaS dashboard. Flat design also keeps the static-HTML approach lightweight if it stays framework-free.
**Revisit if:** The product grows features (real-time updates, notifications) that genuinely need motion to communicate state changes.

## 2026-07-16 — Brand reference is CBGB / dive-bar punk, rendered non-skeuomorphically
**What:** Voice and visual restraint are modeled on Strummer/Cobain/Elliott Smith — DIY, nothing overproduced, no ornament that isn't earning its place. High-contrast black/white, one ink accent (blood red or rust) for overlap only, typewriter-adjacent data type. Explicitly no literal bar/flyer objects — no staples, pushpins, torn-paper edges, or grain textures standing in for a physical space.
**Why:** The punk reference and the "keep it minimal" instinct aren't actually in tension — restraint is the aesthetic. But rendering the metaphor literally (fake staples, distressed paper) tips into skeuomorphism and reads as a theme-park version of a dive bar rather than the real thing. The brand should come from what's absent, not from illustrated props.
**Revisit if:** The flat/no-ornament rule makes overlap or state changes hard to read — may need one restrained affordance (not decorative) to carry that signal.

## 2026-07-16 — Frame: 12-column grid, fixed spacing scale, type tied to spacing rhythm
**What:** Built the layout foundation before any components: a spacing scale (0/4/8/16/24/32/40/48/56/64/72), a 12-column grid that keeps 12 columns at every breakpoint (only margin/gutter change — 16/16 mobile, 24/16 tablet, 32/24 desktop, 1200px max-width), and a 5-size type scale whose line-heights are pulled directly from the spacing scale rather than invented separately. Body copy is capped at a 60–75 character measure via a `.prose` utility rather than allowed to span full width.
**Why:** Requested as the baseline to build components on top of, one at a time (header, body container, footer next). Tying line-heights to spacing tokens means layout and type share one rhythm instead of two systems that drift apart. Keeping columns fixed at 12 and only varying margin/gutter is simpler to reason about than changing column count per breakpoint.
**Revisit if:** A component genuinely needs a type size or spacing value outside these scales — add it to the token file deliberately rather than hardcoding a one-off value in the component.
**Superseded by:** the 2026-07-16 grid entry below — column count now changes per breakpoint (4/8/12) instead of staying fixed at 12.

## 2026-07-16 — Grid moves to 4/8/12 columns, numbered col-N replaced with semantic spans
**What:** Changed the grid from "always 12 columns" to 4 columns mobile / 8 tablet / 12 desktop — Material's standard layout-grid split. Because column count now varies, the col-1…col-12 utilities were replaced with semantic classes (`.col-full`, `.col-half`, `.col-quarter`, `.col-third`) that each carry the correct span per breakpoint, so "half" always means half regardless of how many columns exist underneath it.
**Why:** Requested directly. 4/8/12 share clean common factors — half and quarter divide evenly at every breakpoint, which a flat 12-always grid doesn't guarantee once you're actually laying out narrow mobile screens (a lone "span 3" reads very differently at 375px than at 1280px). Thirds are the one span that only divides cleanly at 12, so `.col-third` is explicitly desktop-only and falls back to full width elsewhere rather than producing an uneven split.
**Revisit if:** A component needs a span these four don't cover — add a new semantic class with its own per-breakpoint values rather than reintroducing raw col-N numbers.

## 2026-07-16 — Display face changed from Oswald to Anton
**What:** Swapped the display typeface (headings, hero numbers, page title) from Oswald to Anton.
**Why:** Oswald read as clean athletic/highway-sign condensed rather than gig-poster. Anton is bolder and blunter, closer to the lettering on an actual show flyer, without introducing any texture or distress — the punk feeling comes from the letterform's weight and bluntness, not from decoration, which keeps it consistent with the no-skeuomorphism rule in the brand brief.
**Revisit if:** Anton reads as too heavy/shouty once real show titles are set in it — Staatliches and Archivo Black are the next things to try, in that order.
**Superseded by:** the 2026-07-16 entry below — moved straight to Archivo Black after seeing Anton rendered in the header.

## 2026-07-16 — Display face changed again, Anton to Archivo Black
**What:** Swapped the display typeface a second time, from Anton to Archivo Black.
**Why:** Anton read as too light/generic once actually rendered in the header — it's also the internet's default "meme caption" font, which works against the intended feel. Archivo Black is the heaviest weight readily available, blunt and block-like rather than condensed-athletic, without adding any texture or distress.
**Revisit if:** Archivo Black's lack of condensed width becomes a problem for longer show titles wrapping awkwardly — Big Shoulders Display (900 weight) is the next thing to try, it keeps a similar heaviness with more condensed proportions.

## 2026-07-16 — Identity model: unguessable slugs + cookie, not accounts
**What:** People and groups are identified by unguessable slug URLs rather than a login/password account system. First visit issues a person-slug stored in a cookie; joining a group associates that slug with a group-slug. The profile screen reads the list of joined groups straight from the cookie rather than querying a server for "who is logged in."
**Why:** Proposed directly, and it's the right call — it kills signup friction entirely (no email, no password, no verification step) while still giving each person a persistent identity within a browser. This is distinct from the backend/database question: a shared datastore for group, show, and response data is still required regardless of identity model, because the overlap mechanic depends on multiple people's browsers reading and writing the same data. What this decision removes is *accounts* specifically — not the need for a server to hold shared state.
**Revisit if:** Someone needs to recover access after clearing cookies or switching devices — likely fix is treating the invite link itself as the recovery path (opening it again re-associates the browser), not adding real login.
**Correction (2026-08-06):** group slugs are no longer unguessable — see the entry below. Person slugs (cookie-stored, never shown in a URL) are unaffected.

## 2026-07-16 — Backend is Supabase, called directly from vanilla JS
**What:** Needed a real backend now (testing with an actual friend group requires shared, persistent data — see prior discussion on why that's unavoidable). Using Supabase (Postgres + auto-generated REST API), called straight from the browser via the supabase-js client loaded from a CDN. No custom server, no Next.js/React, no build step. Schema is 5 tables (people, groups, memberships, shows, responses) in `app/supabase/schema.sql`, matching the slug/cookie identity model exactly. RLS is enabled but left open (anyone with the anon key can read/write any row) — access control comes from slugs being unguessable, not from row-level checks.
**Why:** Supabase's JS client works from a plain `<script type="module">` import, so it doesn't force the vanilla-HTML-artifact workflow to become a framework app — the earlier React-vs-vanilla question resolves itself: stay vanilla, let Supabase be the only "backend" there is. Open RLS is a real tradeoff, but this is a low-stakes friend tool, not something that needs to resist a motivated attacker, and closing it later is additive (write real policies) rather than a rearchitecture.
**Revisit if:** The app ever holds anything more sensitive than show titles and who's curious about them, or if enumeration (a stranger listing every group without a slug) turns out to matter — tighten RLS to require a known group_id/slug at that point.
**Correction:** the `<script type="module">` approach described above was wrong in practice — see the entry below.

## 2026-07-16 — Dropped ES modules, plain scripts + window.OTR namespace
**What:** `index.html` got stuck permanently on "Loading…" — root cause was `<script type="module">` with `import`/`export` between local files. Browsers block module-to-module imports under CORS rules when a page is opened directly (file://) rather than served over http. Fixed by dropping ES modules entirely: Supabase now loads from its UMD CDN build (plain global `<script src>`, defines `window.supabase`), and `client.js`/`identity.js`/`shows.js` are plain scripts that hang their functions off a shared `window.OTR` namespace instead of import/export.
**Why:** "Open the file directly, no build step" is a hard requirement from the project brief, and ES modules quietly break that the moment two local files need to talk to each other — it half-worked (the page loaded, styles applied) which made it a confusing bug rather than an obvious one. Plain scripts have no such restriction and have worked in browsers since file:// existed.
**Revisit if:** The script count under `window.OTR` grows unwieldy — a build step would be the fix at that point, not a return to unbundled ES modules.

## 2026-07-16 — Add-show sourcing: Ticketmaster search first, link-paste second, manual always available
**What:** Built three ways into the Add Show form: (1) search by artist name against Ticketmaster's Discovery API, scoped to New England by state code, results in a clickable picker; (2) paste a link, server-side scrape via JSON-LD/Open Graph (the `parse-show-link` Edge Function); (3) blank manual form. All three converge on the same manual form for review before submit — nothing auto-submits without a human looking at it first.
**Why:** Tested the link-scrape approach directly against real Ticketmaster and AXS URLs first — both came back completely empty (confirmed bot protection, not a parsing bug). Researched alternatives: Ticketmaster has a free, self-serve Discovery API (5000 calls/day) built exactly for search-by-artist, and covers ~80% of *major* venues' primary ticketing per the DOJ's antitrust case against Live Nation (jury found them liable for monopolizing the market, April 2026) — but that figure skews toward arenas/amphitheaters, not the small clubs and dive bars this app is actually about. AXS and StubHub have no comparable public API. So: search is the reliable primary path for bigger shows, link-paste is a free bonus for indie venue sites with their own pages, and manual entry is treated as a first-class method, not a fallback apology, because it's genuinely going to carry a lot of real usage.
**Revisit if:** Coverage of small/DIY venues turns out to matter more than expected — worth researching Eventbrite or venue-direct integrations at that point, since neither Ticketmaster nor a generic scraper serves that segment well.

## 2026-07-16 — Ticketmaster API key stored as an Edge Function secret, not hardcoded
**What:** Unlike the Supabase anon key (hardcoded in `app/supabase/client.js`, safe by design), the Ticketmaster Consumer Key is stored as a Supabase Edge Function secret (`TICKETMASTER_API_KEY`, set via the dashboard) and read at runtime with `Deno.env.get(...)`. It never appears in `app/supabase/functions/search-tickets/index.ts` or anywhere else committed to git.
**Why:** This repo is public on GitHub. Supabase's anon key is explicitly designed to be public (RLS does the actual access control), but Ticketmaster's key isn't — if it were hardcoded and pushed, anyone could lift it from the repo and burn through the daily quota. Costs nothing to keep it as a secret instead, so there's no reason not to.
**Revisit if:** Never, really — this is just the correct default for any third-party API key in a public repo going forward, not a one-off.

## 2026-07-17 — Edge Functions were silently unreachable: wrong slug + JWT verification on
**What:** Both `search-tickets` and `parse-show-link` failed with an identical CORS-preflight error no matter what — turned out to be two stacked issues, not one. (1) Supabase's "Deploy a new function" dialog auto-generates a random name (`dynamic-action`, `smooth-endpoint`) if the Name field isn't set at creation, and that name *is* the URL slug — renaming a function afterward in its Settings tab only changes the display label, the callable URL doesn't change. So the app's calls to `/functions/v1/search-tickets` were 404ing against functions that were never deployed under that name. (2) Separately, "Enforce JWT Verification" defaults to on for new functions, which rejects the OPTIONS preflight itself (no auth header on preflight requests) before the function code ever runs — same generic CORS error in the browser, indistinguishable from the naming issue without checking Edge Function logs. Fixed by redeploying both functions fresh with the correct name typed in at creation and JWT verification off.
**Why:** Both failure modes produce the exact same "preflight doesn't pass access control check: It does not have HTTP ok status" message in the browser, which made this take a while to isolate — the fix required checking the actual Edge Functions list (not just individual function pages) to catch the naming mismatch.
**Revisit if:** Deploying any future Edge Function — always type the intended name at creation time (don't rely on renaming later) and turn JWT verification off immediately if the function is called from the browser via the anon key rather than a real user session.

## 2026-07-17 — Add Show form trimmed to title/date/venue; search and link autofill skip the review step
**What:** Removed Time, Venue Link, and Openers as visible fields from the manual form — down to title (required), date (required), venue name. Venue name now has typeahead autocomplete (new `search-venues` Edge Function, same Ticketmaster-by-New-England-state pattern as `search-tickets`) that silently attaches a venue link on selection with no visible field for it. Separately, artist search and link autofill no longer route through the manual form when they already have a usable title + date — they call `createShow` immediately and the show just appears in the list. The form is now reserved for actual manual entry, or as a fallback when autofill comes back incomplete.
**Why:** Requested directly — the review-before-submit step added friction for the two paths (search, link) that already return trustworthy structured data, and time/venue-link/openers were fields nobody was filling in manually anyway. Auto-submitting only when title + date are both present keeps the original safety concern intact: a show still can't be created from garbage data, it just doesn't force a human click-through when the data's already good.
**Revisit if:** Someone wants to edit time/openers/venue-link after creation — those columns still exist in the schema and still render on the show card when present, this just stopped collecting them through the trimmed form. Add them back as an "edit show" affordance rather than reintroducing them here.

## 2026-07-17 — Show title links to the show page, venue name links to the venue's calendar
**What:** Two distinct links, not one. `source_url` is the show's own ticket/event page (from artist search: the Ticketmaster event URL; from link autofill: the pasted URL itself). `venue_url` is the venue's own page/calendar (from artist search: the venue's Ticketmaster page, via a bug fix — it was wrongly set to the event URL, identical to source_url; from venue autocomplete: the selected venue's Ticketmaster page). The show card title links to `source_url`, the venue name links to `venue_url`. `createShow` was silently dropping `source_url` even though the column already existed in the schema — fixed to actually insert it.
**Why:** Requested directly — title should take you to buy/view the specific show, venue name should take you to that venue's upcoming calendar. These are different destinations and shouldn't share one URL.
**Revisit if:** A show has no `source_url` (pure manual entry, or a link-autofill result that only found a venue link) — title just renders as plain text in that case, same pattern already used for venue name without a link.

## 2026-07-17 — Onboarding: on-page name gate, slugs stay the join mechanism (as a link, not a typed code)
**What:** Replaced the `window.prompt()` name entry with a real on-page form (`#name-gate`) that hides the header/main until submitted. Groups are joined by opening a link containing `?g=<group-slug>`, not by typing an invite code — the group switcher now has a "Copy invite link" button that builds that URL for whichever group is current. No separate "splash page" was built; the name gate is a lightweight form in front of the same page shell, not a distinct marketing screen.
**Why:** Considered a typed invite-code system (Discord/Kahoot-style) instead of link-based slugs, but a typed code only earns its place when a link can't be shared — not the case here, this group shares things over text/iMessage where a tappable link is strictly less friction than typing a code correctly. So the existing slug design didn't need to change, just needed an actual UI for sharing and consuming it, which hadn't been built yet.
**Revisit if:** The app ever needs to support sharing outside a medium that carries links well (read aloud, printed, etc.) — that's when a short typed code would actually earn its complexity.

## 2026-07-17 — List/Calendar view toggle
**What:** Added a segmented List/Calendar toggle above the show list. Calendar is a month grid with prev/next navigation, showing each show on its date (title linked to `source_url`, bold when overlap ≥ 2). Both views share one data fetch so they never disagree with each other.
**Why:** Requested directly — a flat upcoming list works for a handful of shows but a calendar makes date clustering and month-to-month browsing easier to scan at a glance.
**Revisit if:** A single day regularly has enough shows to need "+N more" overflow handling, or the calendar should default to the month of the nearest upcoming show instead of always opening on today's month.

## 2026-07-17 — Accent color: blood red → hot pink → salmon → warm rose-pink
**What:** `--color-accent` went through three swaps in one sitting: `#b3121b` (blood red) → `#ff1493` (hot pink, Clash/Westwood register) → `#e8927c` (dusty salmon, too far toward orange/cream) → `#f2547e` (warm rose-pink, the middle ground between the hot pink and the salmon). Single token every time, so overlap badges, button hover, search-result highlight, and form-focus outline all followed automatically.
**Why:** Hot pink read as too electric; salmon overcorrected too far toward orange/cream and stopped reading as pink. This lands between the two — still warm, still clearly pink first.
**Revisit if:** Never really — swap the token again if the shade needs further adjustment, same pattern as every time here.

## 2026-07-17 — Calendar rebuilt: uniform cells, mobile shrink, pink bubbles, show detail page
**What:** Several related changes in one pass. Calendar cells switched from `min-height` (uneven rows once a day had 2+ shows) to a fixed `height` with overflow capped at 2 visible entries per day and a plain "+N more" beyond that. Cells shrink further below the 600px breakpoint, and weekday headers are single letters at every size, so 7 columns stay legible on a phone. Each show now renders as a small accent-colored dot + time (Google/Apple-calendar style) instead of full title text — there isn't room for a title at this cell size. Because bubbles can't hold a response control, added a show detail page (title/venue/openers/avatars/response buttons — the same block a list card shows) reachable by tapping a bubble, or via a new "View details" link on list cards. The interactive body is one shared function (`renderShowInteractiveBody`) used by both the list card and the detail page.
**Why:** Requested directly — uneven cell heights and full title text weren't going to survive contact with a phone screen, and bubbles alone can't carry an interaction, hence the detail page. Building it as a shared function rather than a second copy of the card markup was the only way to add it without doubling the surface area that has to stay correct.
**Revisit if:** A day regularly needs more than 2 visible shows (make "+N more" actually do something, e.g. expand in place); a show ever needs a shareable direct URL of its own, the way group invite links work today.

## 2026-07-17 — Response states cut back down to two: curious / out
**What:** Dropped "Got tickets" (`going`) entirely. Just two buttons now: Curious, I'm out. Overlap counts `curious` only. Schema's check constraint tightened to `('curious', 'out')`; existing `going` rows need a one-time migration (`app/supabase/migrations/2026-07-17-drop-going-status.sql`) folding them into `curious` before the constraint change, since someone who'd marked "got tickets" is still meaningfully interested.
**Why:** "Got tickets" implied a specific purchase action that doesn't actually map onto how this group buys tickets — sometimes together, sometimes separately, always as a conversation that happens off-platform once people know who else is in. The app's job is surfacing that overlap, not modeling ticket-purchase logistics, so the state describing purchase status didn't earn its place.
**Revisit if:** People want a lightweight "I'm locked in" signal beyond "curious" after all — worth checking whether that's a real gap before re-adding a third state, rather than assuming.

## 2026-07-17 — Name gate splits into two paths, first-group setup moved into it, gate is centered
**What:** The name gate now checks `?g=<slug>` once at load (before it gets stripped) to decide what to show — same tagline either way ("Line up shows with your pals"), but a cold start (no invite link) gets a second field, Group name, right in the same form. One submit: invite-link visitors just create their person and go straight in; cold-start visitors create their person and their first group together, then see a share nudge ("Copy invite link" / "Continue") since that's the moment an invite link is most worth surfacing — the group is otherwise empty. The whole gate is centered in the viewport (`.name-gate-center`, flex both axes) instead of sitting in the normal page grid.
**Why:** Requested directly, and refined once — first pass split this into three separate steps with different taglines per path; correction was to keep one tagline for both and just add the second field to the existing form rather than a whole extra step, which is simpler and faster to fill out.
**Revisit if:** Leaving the group name blank (cold start, field not required) just proceeds into the "no groups yet" empty state — fine as a fallback, but worth watching whether that trips people up in practice.

## 2026-07-17 — Share screen: two real actions front and center, not one primary + a "Continue"
**What:** The post-group-creation share screen now leads with two full-width buttons — "Copy invite link" and "+ Add first show" — instead of one primary action plus a plain "Continue." Skipping past both is still there, just demoted to a quiet text link ("Skip to your group") below them. "+ Add first show" reveals the real app and opens the Add Show panel in the same motion, so the onboarding screen gives way to the normal list as soon as a show actually gets added — no separate transition to build, the existing add-show flow already re-renders the list once a show is created.
**Why:** Requested directly — the original "Continue" button undersold what was actually useful to do right there (share, or seed the group with a first show); centering both real actions makes the empty group's next step obvious instead of just dismissing a screen.
**Revisit if:** Never really — this is just better copy/emphasis on an already-built mechanism, not a new one.

## 2026-07-18 — Invite link moved from the header menu to the main page
**What:** "Copy invite link" no longer lives in the group-switcher dropdown. It's now a quiet text link ("Invite a friend") on the main page itself, above the Add Show panel. Added a `currentGroup` object (not just `currentGroupId`) set in `selectGroup`, so the link is always unambiguously scoped to whichever group is on screen.
**Why:** Requested directly — inside a group-switcher menu, "copy invite link" doesn't make it obvious which group it's for once someone belongs to more than one. Moving it out to the main page (scoped to the group actually being viewed) removes that ambiguity, without turning the show-list header into a second row of buttons.
**Revisit if:** Never really — same mechanism as before, just relocated and unambiguous now.

## 2026-07-18 — Detail page gets price + Buy tickets; Spotify and news considered, cut
**What:** Added `price_min`/`price_max`/`price_currency` to `shows`. `search-tickets` maps Ticketmaster's `priceRanges`; `parse-show-link` makes a best-effort attempt from JSON-LD `offers`. The detail page (not the list card) shows a price line and a "Buy tickets" button linking to `source_url` — a bigger, clearer CTA than the title link alone. Considered adding a Spotify listen link and an artist-news link too; cut both for now — Spotify would've been cheap (a plain search deep-link, no API key needed) but wasn't asked for once price/buy was scoped; news was cut outright as scope creep that doesn't serve the overlap mechanic and would need its own API for real value.
**Why:** Requested directly. Price and a clear buy CTA are real, useful, and already backed by data Ticketmaster's API gives us for free — worth building. News doesn't touch "are my friends into this show," which is the actual point of the app.
**Revisit if:** Someone specifically asks for a Spotify link later — the deep-link version stays a cheap add whenever that comes up.

## 2026-07-18 — Spotify link added, sourced free from Ticketmaster's data — real Spotify API ruled out
**What:** `search-tickets` now also pulls `spotify_url` from Ticketmaster's own attraction data (`_embedded.attractions[0].externalLinks.spotify`) — a verified artist link Ticketmaster already returns in the same response, no separate integration. Rendered as "Listen on Spotify" on the detail page. Checked whether a real Spotify Web API integration was worth it instead (would unlock more than just a link) — ruled out: Spotify's February 2026 changes cap Developer Mode apps at 5 authorized users (a hard ceiling for a friend-group app that's supposed to grow) and removed Top Tracks, Related Artists, and Recommendations for new apps — exactly the endpoints that would've made a real integration worthwhile. Extended quota (which lifts those limits) requires being a registered business with 250k+ monthly active users, obviously out of reach here.
**Why:** The Ticketmaster-sourced link costs nothing and already existed in data this app fetches anyway. A real Spotify integration would cost real setup (API key, Edge Function, secret) for less capability than it would have a year ago, given what's been removed — not worth it at any scope this project could reach.
**Revisit if:** Spotify's dev terms change again, or the 5-user cap stops mattering because Spotify data becomes genuinely core to the product rather than a nice-to-have link.

## 2026-07-18 — Calendar: days with shows get a pink tint, weekday header inverted
**What:** Any day cell with 1+ shows gets a pink-tinted background (`.has-shows`, using the accent color at low opacity) so it reads at a glance before even looking at the bubble inside it. The weekday header row (S/M/T/W/T/F/S) flipped to black background, white text, matching the same inverted treatment already used on active buttons elsewhere.
**Why:** Requested directly — the small dot alone wasn't obvious enough that a day had something on it; a tinted cell is a much bigger, easier target to notice while scanning a month.
**Revisit if:** Never really — small, self-contained visual tweak.

## 2026-07-18 — New MISSION.md: right-sized founding document instead of a full business plan

**What:** Asked for something bigger than the brand brief — mission statement, vision, business plan, principles, leadership team, a board, finance/cost tracking, usage/analytics. Ran a three-perspective review first (see the audit below); pushed back on the mismatch between formal business-plan/board/leadership-team scaffolding and the project's actual state (one person, $0 spend, pre-launch, one friend group) and proposed a scaled-down equivalent instead. Built `MISSION.md` at the repo root: mission, vision, principles (extracted from this log's own recurring "why"), the CBGB-vs-Ticketmaster tension written up honestly with a phase-out aspiration, a plain "it's just Dan" note instead of invented leadership roles, an informal advisors list (reusing the three-perspective review as a repeatable pattern instead of a standing board), a lean cost-watch (real Supabase/Ticketmaster free-tier numbers and the auto-pause-after-7-days gotcha), a lean usage-check (raw Supabase table checks, no analytics platform), and a speculative, explicitly-deferred monetization section (affiliate revenue on ticket click-throughs is the lowest-friction option, since Ticketmaster does run a real affiliate program through Impact).
**Why:** Requested directly, but the literal ask (full business plan, leadership team, board) would have meant inventing structure a one-person hobby project doesn't have yet. The right-sized version keeps the actual value (a clear mission/vision/principles reference, honest tension-naming, a real cost ceiling to watch, a way to tell if anyone's using it) without fictional corporate scaffolding.
**Revisit if:** The project ever actually has more than one person building it, or real revenue — that's when a leadership section or a real monetization decision (not just a speculative list) would earn its place.
**Revised same day:** first draft's principles were build/design rules (flat visuals, no accounts, ship-simple) that belonged in the brand brief or this log, not a vision doc — rewritten to be about what the product owes the people using it (overlap as signal not taste-ranking, everyone's input counts equally, it's a signal not a replacement for the group, no manufactured urgency, as low a bar to join as a group chat). The Ticketmaster tension also got resolved rather than left open: CBGB wins over convenience for this document specifically — Ticketmaster stays a stopgap data source, owning an independent venue/show data network is the stated long-term aim, build specifics deferred. "Who's building this" section cut entirely (added nothing). Monetization section corrected: affiliate revenue on ticket click-throughs is viable now, not deferred — gated only on Dan applying to Ticketmaster's affiliate program (a real signup/account step), not on product changes or future scale; a plain disclosure line to the group is recommended if it's ever switched on, though not legally required for a private app.

## 2026-07-18 — brand-brief.md corrected to match; new user-flows.md written

**What:** Fixed brand-brief.md's Audience section, which still said "genuinely different music taste" — the actual framing (per the MISSION.md correction above) is that the group already trusts each other's ear because everyone's watching different venues/artists and someone's always finding out first, not because their taste differs. Added a short Reference-points note mirroring the CBGB-wins-over-Ticketmaster call. Also wrote `02_Product/user-flows.md`: a plain task-by-task inventory of what's actually built (getting in, the curious/out loop, three add-show methods, list/calendar/detail browsing, group management) plus an honest "not built" list (no editing/deleting a show, no leaving a group, no notifications, no history) — written to size up the product's actual current scope, not as a spec.
**Why:** Requested directly, both to keep the founding docs internally consistent and to get a clear read on how big this thing actually is before deciding what (if anything) to add next.
**Revisit if:** Any of the "not built" list gets added — update user-flows.md alongside the code change, same as component-map.md.

## 2026-07-18 — Reversed: sticking with Ticketmaster, not aiming to replace it

**What:** Walked back the "CBGB wins, own an independent data network is the long-term aim" call made earlier the same day in MISSION.md and brand-brief.md. Both now say the practical call is to keep using Ticketmaster's API — it's genuinely the only realistic free option for artist search right now — while still naming the brand mismatch honestly. No stated aim to replace it later.
**Why:** Needed a working search path now; committing to phasing out the only viable data source wasn't a real plan, just a values statement made before weighing the practical need.
**Revisit if:** A comparable free/self-serve alternative ever turns up — worth a fresh look then, not before.

## 2026-07-18 — Venue links stay pointed at Ticketmaster's venue page, not each venue's own site
**What:** Raised wanting venue links to go to "legit venue sites" rather than Ticketmaster. Left as-is for now: Ticketmaster's Discovery API (both `search-tickets` and `search-venues`) doesn't expose a venue's own official website as a field — only its own Ticketmaster venue page, which is why that's what gets stored. Getting the venue's real site would need a separate lookup (a web search per venue, or a hand-maintained venue-name-to-URL table), which is real complexity for something that already works as a de facto calendar.
**Why:** No cheap fix exists on Ticketmaster's side. Manually-entered venues and link-autofill from a venue's own page already produce their real site when the data's there, so the gap is specifically "shows added via Ticketmaster search."
**Revisit if:** This bothers Dan enough in practice to justify a small hand-maintained table of known New England venues → real site — worth reconsidering only if it turns out to matter more than it seems right now.

## 2026-08-12 — Delete a show; only the person who added it can

**What:** Added `OTR.deleteShow(showId)` (`app/scripts/shows.js`, a plain `.delete().eq('id', showId)`) and a top-right trash icon on each show card, rendered only when `show.created_by === person.id`. Confirms via a plain `window.confirm()` before deleting — same "reach for the browser's own primitive" approach `Leave herd` already used, no custom modal, no destructive-red button (this app still has no destructive-red token anywhere).
**Why:** Requested directly — closes the oldest gap on `user-flows.md`'s "not built" list. Restricting it to the creator (rather than any group member) keeps it simple for now: `groups` has no owner concept, but `shows.created_by` already exists and is the obvious boundary — nobody wants a friend able to delete a show *they* added just because they're in the same herd.
**Revisit if:** Someone other than the adder needs to remove a bad/duplicate entry (e.g. spam, wrong date) — would need either a "any member can delete" relaxation or a lightweight moderation concept, neither of which exists yet.

## 2026-08-12 — Rename a herd, pencil next to the name

**What:** Added `OTR.updateGroupName(groupId, name)` (`app/scripts/identity.js`, mirrors `updatePersonName`) and turned the group modal's title into a name + pencil button (`.group-modal-title-btn`, same shape as the drawer's own `.drawer-name-btn`) that reveals an inline text input, saving on blur/Enter. Any member can rename — `groups` has no owner concept, same reasoning `Leave herd` already relies on. Deliberately leaves `groups.slug` untouched on rename, so every invite link already sent out (and the group's own `?g=` URL) keeps working.
**Why:** Requested directly — the pencil-next-to-name pattern already existed for editing your own display name, so this reuses it rather than inventing a second edit affordance.
**Revisit if:** Slug-from-name ever needs to actually track a rename (e.g. someone wants the URL to reflect the new name) — that's a bigger, deliberate call given it would break existing links, not a default to reach for.

## 2026-08-06 — Group slugs: readable name, not an unguessable hash
**What:** `groups.slug` (the `?g=<slug>` in every invite link) switched from a random 12-byte hex string — the DB's own default (`schema.sql`) — to the group's own name, slugified client-side (`boston-crew`), with `-2`/`-3`/... appended on a name collision. Generation moved from an implicit Postgres column default to explicit logic in `OTR.createGroup` (`app/scripts/identity.js`), which now passes `slug` on insert and retries on a unique-constraint violation (Postgres code `23505`) rather than letting the DB pick silently.
**Why:** Requested directly, after seeing a live invite link (`gigherd.com/?g=098aea86c5eb9dc6461b166f`) called "crazy" — a raw hash undercuts the whole point of a friendly, low-friction invite link. This does trade away some of the "unguessable" property the 2026-07-16 identity-model decision leaned on, but that decision's own reasoning already named this app as "low-stakes... not something that needs to resist a motivated attacker" — a readable link was judged worth the small amount of guessability it costs. `joinGroup` still has no invite-approval step, so someone who correctly guesses a common group name (and knows to try it) could self-join; acceptable at friend-group scale, not acceptable if the app ever holds anything more sensitive.
**Also:** the name-gate's invite-link path now shows the actual group name ("You've been invited to join 'Boston Crew' — add your name and jump in.") instead of the generic tagline, via an early `getGroupBySlug` lookup at page load (separate from the one `handleGroupJoinFromUrl` does later to actually join). Deliberately doesn't attribute the invite to a specific person ("X added you...") — the link is one per group, not per-sender, so there's no reliable "who shared this" to name; a wrong attribution would be worse than none.
**Revisit if:** Group-name collisions turn out to be common enough that `-2`/`-3` suffixes get ugly in practice, or if guessability ever becomes a real problem — tighten by requiring an explicit join-approval step rather than reverting to opaque slugs.

## 2026-08-15 — Browse feature: curated link-out directory by venue category, not hosted calendars

**What:** New "Browse" feature, Airbnb-style category menu as the entry point: **Tiny venues, Small venues, Classic theatres, Arenas & Stadiums, Outdoor, Legendary, Festivals**. Each category lists hand-picked venues; clicking a venue opens its own calendar/events page in a new tab. No scraping, no in-app hosting of show listings — this is a curated directory, not a database of events.
**Why:** Directly follows from the 2026-07-16 add-show entry above, which already confirmed Ticketmaster and AXS both block scraping (bot protection, not a parsing bug) — so hosting listings for major venues isn't realistic regardless of effort. Small/DIY venues are more mixed (some have plain HTML calendars, a few might expose iCal/RSS, most are Squarespace/Wix widgets with no clean underlying data), but even where scraping is technically possible, hosting takes on an ongoing staleness risk (rescheduled/cancelled shows going wrong on-site while the venue's own page is right) that linking out avoids entirely. "Legendary" is a deliberate cross-cut, not a strict tier — a venue can be both tiny and legendary, and categories are allowed to overlap. Arenas and Stadiums were combined after discussion — the size distinction didn't feel worth a separate category.
**Revisit if:** Worth checking whether any of the existing Boston/Cambridge venue list actually expose an iCal/RSS feed — that's the one case where hosting real listings (venue-by-venue, not uniformly) would be cheap and low-maintenance rather than a scraping project. Otherwise, link-out stays the default indefinitely.

## 2026-08-15 — Browse starts Boston/Cambridge-only; Search stays New England-scoped, both scale up together later

**What:** Browse launches with a small hand-curated Boston/Cambridge metro list (roughly 25-40 venues across the seven categories) rather than full New England — matches the 7 venues already verified in the prototype. Artist/venue Search was double-checked against the 2026-07-16 and 2026-07-17 entries and confirmed to already be scoped to New England by state code, not nationwide — so Search and Browse end up aligned in scope rather than one being wider than the other. No change made to Search's scoping.
**Why:** Raised as "does Search cover any show anywhere" — it doesn't currently, and broadening it to nationwide was considered but not chosen, since New England already matches the group's actual usage and keeps results relevant. Starting Browse at Boston/Cambridge rather than full New England keeps the curated list a real, maintainable size at launch.
**Revisit if:** The group starts wanting shows/venues outside New England regularly — at that point Search's state-code scoping and Browse's city-by-city expansion should be widened together, not just one of the two.
**Superseded by:** the entry below — Browse scoped to full New England from the start instead of Boston/Cambridge-only.

## 2026-08-15 — Browse scoped to full New England from the start, matching Search

**What:** Reversed the entry above — Browse launches covering all of New England (not just Boston/Cambridge), aligned with Search's existing state-code scoping. Realistic size at that scope: roughly 60-100 venues across the seven categories, pulling in Providence, Portland ME, Burlington VT, New Haven/Hartford CT, and Manchester NH alongside the Boston/Cambridge core.
**Why:** Requested directly — keeping Browse and Search at the same scope from day one avoids the mismatch where Search can find a show anywhere in New England but Browse can't point you to the venue that's hosting it. Trades a bigger upfront curation task for scope consistency.
**Revisit if:** The 60-100 venue curation turns out to be too much to hand-build and maintain at once — could still launch Boston/Cambridge first as a practical phase-in, expanding city by city, without changing the New England target.

## 2026-08-15 — Browse secondary facets: state, city/metro, all-ages vs. 21+; day-trip/weekend and genre rejected

**What:** Beyond the seven venue categories, Browse also tags each venue by **state** (ME/NH/VT/MA/RI/CT), **city/metro** (Boston, Providence, Portland, Burlington, New Haven/Hartford, Manchester), and **all-ages vs. 21+**. Considered and set aside: a day-trip/weekend-trip distance framing (too hard to define cleanly) and genre-leaning tags (too close to the mission doc's "no algorithm ranks taste" principle, risk of drifting from a plain directory into curated recommendations).
**Why:** Requested directly. State and city/metro are useful once the list is 60-100 venues rather than one metro; all-ages vs. 21+ is a genuinely practical filter (determines whether a show's even an option) rather than organizational flavor. Day-trip/weekend and genre were both raised and dropped as not worth the added hand-tagging complexity for a curated (not scraped) list.
**Revisit if:** The venue list grows large enough that these three facets aren't enough to navigate it — genre tagging could be reconsidered at that point if it's framed as a plain descriptive label ("known for punk/indie shows") rather than anything that ranks or recommends.

## 2026-08-15 — Browse venue record adds address, capacity, and transit; skips neighborhood and ticketing-platform

**What:** Each `OTR.VENUES` entry (`app/data/venues.js`) also carries **address** (full street address), **capacity** (a number, or `null` when no figure is published anywhere citable — not a guessed one), and **transit** (nearest MBTA line + station, only set when it's a genuine ~10-min-or-less walk; left `null` for venues that aren't realistically transit-accessible, e.g. suburban amphitheaters, rather than naming a technically-nearest stop that's actually miles off). Neighborhood and ticketing-platform (Ticketmaster/AXS/independent) were both considered and dropped.
**Why:** Requested directly. Capacity gives a feel for the room beyond the category label, and was mostly already gathered during Batch 1's research so cheap to keep. Address was wanted just to have on hand. Transit was scoped down from "always show nearest stop" to "only when it's actually walkable" — the Great Woods/suburban-amphitheater case is exactly the kind of venue where a technically-nearest-but-miles-away T stop would be misleading, not useful. Neighborhood was judged not to add enough value to justify the tagging work. Ticketing-platform was raised as connecting Browse back to Search/add-show, but dropped as not worth the upkeep for what it'd add.
**Revisit if:** Ticketing-platform could be worth reconsidering later specifically to help distinguish "Search will already find this venue" from "only reachable through Browse/manual add" — not pursued now, but the reasoning for it wasn't wrong, just deprioritized.

Rough capacity bands used for the primary categories: **Tiny venues** ≲250, **Small venues** ~250–1,500, **Arenas & Stadiums** 5,000+. Classic theatres and Outdoor are defined by physical character rather than a strict capacity band.

## 2026-08-15 — Browse: est. year added to every venue; notable historical shows added, press-sourced, only where well-documented

**What:** Every `OTR.VENUES` entry now has **established** (year opened, number — or annotated when the space's history predates the current name, e.g. Brighton Music Hall est. 2011 under that name, room itself older as Harpers Ferry). Two of Batch 1's Legendary-tagged venues (Club Passim, Paradise Rock Club) also got **notableShows**: 2-3 short, press-sourced historical notes each (Joan Baez/Dylan/Raitt at Club Passim; U2's Dec 1980 early US gig, a photographed 1978 James Taylor/Carly Simon show, and Pixies at Paradise Rock Club).
**Why:** Requested directly, framed as "big names that played there, always cool to see." Est. year is cheap and static, and does real work for the Legendary tag specifically. notableShows is a heavier lift — deliberately framed as documented history ("X played here in Y," sourced to real press/venue material), not a ranked "best shows" list, to stay clear of the mission doc's "no algorithm ranks taste" principle. Scoped to a trial on the two venues where it's genuinely well-documented, rather than forced onto all seven — most of Batch 1 has nothing citable here, and guessing would be worse than omitting it.
**Revisit if:** The trial feels worth it once Browse is actually being used — expand notableShows to future Legendary-tagged venues city by city, same rule: only add when there's a real press citation, skip otherwise rather than inventing filler.

## 2026-08-15 — Browse UI: nav entry point + prototype iteration, landed on landing chips + 3 distinct sub-pages

**What:** Built `app/browse-prototype.html`, a standalone prototype (not wired into the live Supabase-backed `index.html`) using the real design tokens/components and Batch 1 venue data, to see the Browse feature before committing it to the real build. Went through several shapes before landing:
1. First pass: a flat 7-category grid (one tile per category, tap through to a venue list). Surfaced the Arenas & Stadiums/size-vs-type tension directly (see the "Browse secondary facets" entries above).
2. Second pass: split Size (Tiny/Small/Arenas & Stadiums) and Type (Classic theatres/Outdoor/Legendary/Festivals) into two dimensions, plus State/Metro/Ages as facets — tried as a sequential 5-step wizard, then as a 2-level facet-menu-to-chip-chooser (jump between facets freely), before landing on the final shape below.
3. **Final shape:** landing page has 3 big chips — **By Size**, **By State/Region** (State and Region combined into one, not two separate facets), **By Type**. By Size and By State/Region each open a horizontal scrollable chip strip across the top (Size: Tiny/Small/Arenas & Stadiums, starting on Tiny; Region: alphabetical, one chip per metro since a metro already implies its state in this data) with a live results list below that updates as you tap between chips — single-select. By Type breaks pattern into a vertical list starting with Outdoor, multi-select (a venue can be Legendary AND Outdoor at once). All-ages/21+ was tried folded into the Type list as a 5th toggle row, then cut entirely — "too complicated." There is currently no ages filter anywhere in Browse; `allAges` still displays on every venue card, just isn't filterable.
   The nav entry point itself: "Browse Venues" sits directly under "My Shows" in the drawer, reusing `.drawer-my-shows-btn`'s exact classes rather than a new pattern, per the original ask ("nav item under my shows").
**Why:** Each iteration was requested directly, in sequence, based on clicking through the previous version. The size-vs-type split resolved the MGM Music Hall categorization problem from Batch 1 (see the "Browse feature" entry above) — it's cleanly "Arenas & Stadiums" size with no type tag, rather than forced into one flat list it didn't fit. Landing on 3 broad entry chips rather than 5 individual facets reduces how much the "New England, one metro built so far" limitation shows — State/Region collapsed into one avoids two thin, mostly-empty steps.
**Revisit if:** Worth re-testing the State/Region combination once a second metro (Providence, most likely next) exists — right now it's unverifiable whether combining them holds up with real variety, since Batch 1 only has one region. If an ages filter turns out to be wanted after all, it should get its own simple treatment rather than being bolted back onto the Type list.

## 2026-08-15 — Duplicate shows join instead of splitting; "Nice, X added that too!" toast

**What:** Added `OTR.findMatchingShow(shows, fields)` (`app/scripts/shows.js`) — best-effort duplicate detection: same `show_date` and same normalized (trim/lowercase) `title` always counts as a match; `venue_name` only has to agree when both sides actually have one set (so a manually-typed show with no venue can still match a search result that has one). Not fuzzy beyond that — "Paradise Rock Club" vs "...presented by Citizens" won't match. Wired into `submitShowDirectly` in `index.html` (the single choke point both the search-result path and the manual-form path already funnel through): before creating a show, it re-fetches the group's current shows and checks for a match. If found, no new row gets created — the person just gets a `curious` response on the existing show (same `setResponse` upsert the app already had), and a new toast (`app/styles/components.css` `.toast`, shown via `showToast()`) says **"Nice, {adder's name} added that too!"**, reading the adder's name off the existing show's own `created_by` via the responses already being fetched. Two edge cases handled distinctly: if the current person is the one who originally added it, the toast says "You already added that one!" instead of naming them to themselves; if they're already marked curious on it, "You're already in on that one!"
**Why:** Requested directly — "if someone adds the same show/event the cards should join together... is there a way we can say 'nice x name added that as well!'". No schema change was needed: `shows.created_by` already made "first adder is the owner" true (see the 2026-08-12 delete-permission entry), and `responses` was already the per-person overlap mechanism — the only real gap was that adding a show never checked for an existing match first, so two people independently adding the same show produced two cards splitting the overlap signal in half instead of one card with two names on it, which defeats the actual point of the app. The toast's pink accent is a deliberate use of the "overlap means pink, nowhere else" rule from tokens.css, not an exception to it — this message only ever fires at the moment an overlap actually happens.
**Revisit if:** The venue-name exact-match requirement causes real misses in practice (e.g. the same show added once via Ticketmaster search with its formal venue name, once manually with a shorthand) — worth a looser venue comparison at that point, not now, since it's unverified whether this is a real problem yet. Matching is also scoped to shows within the same group only (shows are group-scoped in the schema) — two different friend groups adding the same real-world show correctly stay two separate cards, not merged.

## 2026-08-15 — Browse prototype redesign: less tagging, real type hierarchy, no more accent misuse

**What:** Passed `app/browse-prototype.html` for a design pass, no structural/UX changes — same 3-chip landing → 3 sub-views shape, same data. Four changes:
1. Venue cards dropped the full category-pill tag row entirely (previously every card repeated all of its categories as chips, redundant with whichever facet you just filtered by to get there). A card now shows: name, address (already includes city/state, so it's not repeated separately), one meta line (capacity/est. year/all-ages, dot-separated), transit if present, and — the one thing worth keeping — a single preview line of the venue's *first* notable-history item with a plain "+N more" instead of the old bulleted list.
2. Legendary venues get a small neutral star icon next to the name instead of a pill — same information, no color used to carry it.
3. Fixed a self-inflicted token misuse from the original prototype build: chip-strip "selected" state and Type-list "selected" state were both using `--color-accent` (pink), which directly contradicts tokens.css's own documented rule ("pink is back to meaning one thing again, just logo + overlap, not logo + overlap + every hover state too" — see the color-token comment block). Neither a selected filter chip nor a selected Type row is an overlap event. Both now use the same `--tint-active-strong` fill `.drawer-my-shows-btn.active` already uses for "you're here," with a weight bump for legibility instead of color.
4. Landing page's 3 entry chips no longer use a bespoke "big tile" component (icon circle + oversized label) — they're now literally `.drawer-my-shows-btn` rows, the same component already used for the drawer's own "My Shows"/"Browse Venues" entries and for the Type list. One row pattern for "go somewhere else in the app," reused three times instead of invented three ways.
**Why:** Requested directly — "the design looks terrible... take some inspiration from solid design experiences like instagram, facebook, airbnb, google, slack... dont go so crazy with tagging on these cards, and lets get better about type hierarchy and spacing, and use tokens whenever possible." Re-tested via the same local Playwright harness used for the original functional test (drawer open → landing → each sub-view → filtering) to confirm nothing broke structurally during the pass — filtering, results counts, and the Legendary icon all still render correctly.
**Revisit if:** Once this merges into the real `index.html` (still pending, per the "prototype first, then merge" workflow), re-check whether the single-notable-show preview line still reads as substantial enough once there are venues with 3 historical notes instead of 2 — may want a lightweight "see all" instead of the plain "+N more" text at that point.

## 2026-08-15 — Icons self-hosted instead of loaded from the Lucide CDN

**What:** Added `app/scripts/icons.js` — a small local module inlining the ~19 specific Lucide glyphs the app actually uses (`arrow-up-right`, `check`, `chevron-left`, `chevron-right`, `map-pin`, `pencil`, `ruler`, `share`, `star`, `train-front`, `trash-2`, `users`, `x`, `mic-2`, `music`, `building-2`, `trees`, `landmark`, `tent`), pinned to the same `lucide-static@1.29.0` source the CDN script was already pinned to — no visual change, just no more network dependency. It defines `window.lucide.createIcons(...)` with the exact same call signature the CDN build had, so `index.html` and `browse-prototype.html` didn't need any changes beyond swapping the `<script src>` tag from `unpkg.com/lucide@1.29.0/...` to `scripts/icons.js`.
**Why:** Requested directly, after testing surfaced the underlying issue — the CDN script was an unguarded dependency: the inline script's `lucide.createIcons()` call had no try/catch, so if a user's network or an ad-blocker ever blocked `unpkg.com`, the call would throw and halt page execution entirely, breaking the *whole app*, not just the icons (this is literally what happened during the Playwright test run in the cloud sandbox, which has no route to unpkg.com). The app uses a tiny fraction of Lucide's full icon set, so pulling the whole library from a CDN was already more dependency than needed — self-hosting just the glyphs in use removes the failure mode entirely rather than papering over it with a try/catch.
**Revisit if:** A new icon gets added to the app — `scripts/icons.js` needs the new glyph added to its `ICONS` map (it `console.warn`s instead of silently breaking if a `data-lucide` name isn't found, so a missed addition is at least visible in devtools, not a silent blank icon). Note: fonts (Big Shoulders Display / Public Sans) are still loaded from Google Fonts and not yet self-hosted — same class of fragility, just not in scope for this pass since only icons were requested.

## 2026-08-15 — Browse prototype: fixed star's double meaning, Type now a chip strip, tightened sub-view header

**What:** Three changes to `app/browse-prototype.html`, from direct feedback after seeing it running:
1. The landing page's "By Type" row used a star icon — the same glyph already used on venue cards to mark Legendary venues, so it read as if "By Type" meant "by Legendary." Swapped it for `tags` (added to `scripts/icons.js`, the app's 20th self-hosted glyph). Star still means Legendary, and only Legendary, everywhere in the file.
2. By Type switched from a vertical list of rows to the same horizontal chip-strip carousel as By Size and By State/Region — one consistent interaction pattern across all three sub-views instead of Type being the odd one out. Still multi-select (tapping one chip doesn't deselect the others — a venue can be Legendary and Outdoor at once); Size/Region stay single-select.
3. Tightened the sub-view header: the back button used to be its own text-label row ("‹ Browse") stacked with its own bottom margin, then a separately-margined heading below it — read as "floaty," two loosely related things rather than one unit. Now it's a single compact row: an icon-only circular back button (matching the drawer's own arrow-circle sizing) sitting directly next to the heading. Also trimmed the page's default top padding slightly (prototype-only override; components.css itself untouched) so the header doesn't sit in as much dead air below the site header.
**Why:** All three were called out directly after clicking through the running prototype — "whats the star mean," "by type should be a horizontal carousel like size," "this nav and title seems so floaty... make this tighter, more decided." Re-verified via the Playwright harness: Type chips render, multi-select keeps multiple chips selected at once, the back button is icon-only, and the landing By Type icon is confirmed `tags` not `star`.
**Revisit if:** Once a second/third notable-history-bearing category exists beyond Legendary, worth checking whether a single star is still legible enough on its own or needs a label — not an issue yet with only one category using it.

## 2026-08-15 — Browse: dropped the "New England" subtitle, cut "Tap any that apply," confirmed Type stays multi-select

**What:** Three small cuts to `app/browse-prototype.html`:
1. Removed the "New England" line under the "Browse Venues" heading on the landing page, and simplified the drawer nav item's own sub-copy from "By category, New England" down to just "By category." The scope itself (New England, starting with Boston/Cambridge) is unchanged — see the earlier scoping decisions above — this only removes it as visible UI copy on the Browse pages themselves.
2. Cut "Tap any that apply" from the By Type view.
3. Confirmed (no code change): By Type stays multi-select, does not become single-select tabs like Size/Region. Categories genuinely overlap — a venue can be Legendary and Outdoor at the same time — so a tabs-style "pick one" pattern would misrepresent the data.
**Why:** Requested directly — "nav subtitle should be fixed it says new england it can be more blanketing... I don't think its needed here," and "cut tap any that apply." The Type-as-tabs question was raised and then self-answered in the same message ("damn i guess legendary overlaps") — worth logging so it doesn't get re-litigated later without the context: Type can't be single-select without losing real information about venues that belong to two categories at once.
**Revisit if:** If the Browse scope stops being "just New England" (e.g. once other regions of the country get added), the geographic scope may need to become visible again somewhere in the UI, just not necessarily back on this subtitle line.

## 2026-08-15 — Drawer nav item copy: "By category" replaced with "Find a venue by size, region, or type"

**What:** The "Browse Venues" drawer nav item's sub-line (directly under "My Shows" in the drawer) went from "By category" to "Find a venue by size, region, or type."
**Why:** Called out directly as a "terrible description" right after the previous entry's "By category" simplification — fair, it described nothing a person could act on. The new copy names the three actual entry points (Size/Region/Type) instead of the vague umbrella term "category," matching how "My Shows"'s own sub-line ("Everything you've marked, across your herds") describes what you'll actually see, not just a category label.
**Revisit if:** None expected — this is copy-only, no structural change.

## 2026-08-15 — Browse: added Favorites, top of the landing page, with a heart toggle on every venue card

**What:** Added a fourth entry to the Browse landing page — **Favorites** — sitting above By Size/By State-Region/By Type (per direct spec: "as a category at the top"). Every venue card, in every view, now has a heart-shaped toggle button (top right, next to the venue name) to save/unsave it. The Favorites view itself has no filter chips, just the saved list, with its own empty state ("No favorites yet — tap the heart on any venue to save it here") distinct from the other views' "more venues coming" empty state. The landing row's own sub-copy updates live — "Venues you've saved" when empty, "N venues saved" once you've favorited something — so the count is visible without opening the view. Favorited state is a single in-memory `Set` (venue name as the key) — matches this file's existing "prototype only, not wired to Supabase" scope; nothing persists across a reload yet.
**Why:** Requested directly — "lets add favorites as a category at the top and a button to add to favorites." The heart's on/filled state deliberately does **not** use the pink accent — same reasoning as the chip-strip/Type-row fix earlier in this log: pink means overlap and nowhere else, and the conventional "red heart" pattern would be exactly that misuse again. Uses the same solid-vs-outline, neutral-fill (`--color-text`) language every other "on" state in this file already uses.
**Revisit if:** This needs real persistence once it graduates out of the prototype — favoriting is inherently per-person (unlike `responses`, which is already per-person via `person_id`), so it likely needs its own table (`favorites: person_id, venue_slug` or similar) rather than reusing anything in the current schema. Venue records don't currently have a stable id/slug of their own (see `app/data/venues.js` — keyed loosely by name) — that's worth adding before this merges into the real build, since matching favorites by display name alone is fragile once venue names get edited or two venues share a name.

## 2026-08-15 — Data cleanup: merged a real duplicate show in "Music People"

**What:** Found and merged one genuine duplicate in the live "Music People" herd — two rows for The Mary Wallopers, same date (2027-03-06), same venue, created independently before the duplicate-join feature (see the "Duplicate shows join instead of splitting" entry above) existed to catch it automatically. Kept the older row, moved any responses from the newer one onto it, deleted the newer duplicate — same keep-oldest/merge-responses logic the app's own `OTR.findMatchingShow` uses, run by hand via SQL in the Supabase editor (two plain `insert ... on conflict do nothing` / `delete` statements — an initial version using a temp table across two paste-and-run statements failed with `relation "show_merge_map" does not exist`, because Supabase's SQL editor doesn't guarantee the same DB session across separate statement executions; rewritten as two fully self-contained statements with no cross-statement state).
**Why:** Asked directly — "if there are overlapping shows in my group music people can you join them?" I don't have a path to query/write the live Supabase database directly from this environment (the cloud sandbox's network doesn't reach supabase.co, and the local device-bridge tools have no network access either), so this was done by walking through the SQL by hand in the Supabase dashboard rather than automated.
**Revisit if:** This was a one-off manual cleanup for pre-existing data, not a recurring process — new duplicates going forward are already prevented at add-time by the shipped feature. If this kind of manual cleanup comes up again, the two-statement (no temp table) SQL pattern from this entry is the one to reuse.

## 2026-08-15 — Browse merged into the live app, no longer a standalone prototype

**What:** `app/browse-prototype.html` is retired as a separate file (kept on disk for now, but no longer the real thing) — Browse Venues is now wired directly into `app/index.html`, the live Supabase-backed app. Concretely:
- New `app/styles/browse.css` (all the Browse-specific CSS, carried over from the prototype's inline `<style>` block, generalized from a `.show-list` override to its own `.browse-view` class so it doesn't touch the shows UI's own spacing).
- New `app/scripts/browse.js` (all the Browse view logic — landing/Favorites/Size/Region/Type — loaded as a plain `<script>` after index.html's main inline script, so it can reference that script's already-declared globals directly: `mainContent`, `trackEvent`, `openDrawer`/`closeDrawer`. Same "one shared scope across classic script tags" pattern the rest of the app already relies on, e.g. `supabase/client.js` defining `OTR.db` for `scripts/shows.js` to use.).
- "Browse Venues" drawer nav item added directly under "My Shows" in the real drawer (was previously only in the prototype's copy of the drawer markup).
- `#browse-view` added as a sibling to `#main-content` (the shows UI), not nested inside it — toggled between the two wholesale (`mainContent.style.display`) rather than trying to make Browse coexist with the group heading / Add Show card / show list DOM at the same time. `selectGroup()` and `renderMyShowsView()` (the two existing functions that already switch between "a herd" and "My Shows") now also hide `#browse-view` and clear `#drawer-browse-btn`'s active state at the top, so navigating to either from Browse correctly restores the shows UI — same direct-DOM pattern those functions already used for `drawerMyShowsBtn`.
- `app/data/venues.js` now loads in the real app too. Still a static, curated file — Browse was never meant to be Supabase-backed (see the original "curated link-out directory, not scraped/hosted" decision) — "hooking up the data" here means wiring the feature into the live app, not connecting venues to the database.
- Favorites upgraded from the prototype's in-memory `Set` to `localStorage` (key `gigherd_favorite_venues`), so it survives a reload now. Still per-browser, not per-person/cross-device — the real per-person version (its own Supabase table, keyed by a stable venue id) is still an open item, noted in the Favorites entry above; localStorage was the pragmatic default to ship something better than "resets on every refresh" without committing to a schema decision that hasn't been made yet.
- Minor analytics parity added, matching the app's existing `trackEvent()` pattern: `browse_viewed` (opening Browse), `venue_favorited`/`venue_unfavorited`, `venue_calendar_clicked`.
**Why:** Requested directly — "liking the browse situation. can we hook up the data and bring it into the live tool?" — the "prototype first, then merge" workflow this feature has followed since it started (see the very first Browse decision entries) reaching its last step. Verified with the same Playwright harness used throughout this feature's development, now pointed at the real `index.html` instead of the prototype: drawer button renders and opens Browse, `#main-content`/`#browse-view` swap correctly in both directions, landing order is still Favorites-first, favoriting persists to `localStorage` across the check, and leaving Browse via "My Shows" correctly restores the shows UI with no lingering active state. Supabase itself isn't reachable from this environment, so the surrounding app's real Supabase calls were stubbed for the test — Browse's own code doesn't touch Supabase at all, so this only needed to prove it doesn't break anything Supabase-dependent around it, not that Supabase itself works.
**Revisit if:** A brand-new person with zero herds yet won't see "Browse Venues" in the drawer — it lives inside `#group-bar`, which stays hidden until someone has at least one herd (see `afterPerson()`). Browse doesn't actually need a herd to be useful; worth hoisting it out of `#group-bar` later so it's visible even at zero herds, but that touches the cold-start branch and felt like its own decision rather than something to fold into this merge. Also: `app/browse-prototype.html` itself should probably get deleted once this is confirmed working in production, rather than left around as a second, now-stale copy of the same UI.

## 2026-08-15 — Drawer: fixed nav item spacing, unlocked the footer from the bottom edge

**What:** Two small drawer fixes, both in `app/styles/components.css`:
1. `.drawer-my-shows-btn` (used by "My Shows" and "Browse Venues") had a `margin-bottom` of `--space-16`, a full step looser than the `--space-8` gap `.drawer-group-list` uses between herd rows directly below it — so the top two nav rows read as a visually different rhythm than the herd rows in the same drawer, one list on top of another rather than one consistent column. Changed to `--space-8` to match (also matches `.landing-row` in `browse.css`, which already used `--space-8` reusing this same button class).
2. `.drawer-footer-section` (copyright + contact email) used to be `position: absolute`, anchored to the drawer's own fixed box and locked 48px above the bottom regardless of scroll position. That meant it was pinned to the *viewport*, not to the end of the content — with enough herds to fill the drawer, the herd list or "Add a herd" button would scroll up underneath it and the footer would sit on top of that content instead of below it. Moved back into normal document flow as the last element in the drawer (`margin-top` replacing the old fixed 48px-from-bottom offset), and removed the now-unneeded doubled `padding-bottom` hack on `.nav-drawer` that existed only to reserve room for the old locked footer.
**Why:** Requested directly — "vertical spacing in the nav items is off lets clean it up. and. want to make sure the footer language there is not locked becuaase if the content runs into it it should not overlap. things should jsut push down." Verified with a Playwright screenshot injecting 10 fake herd rows and scrolling the drawer to its bottom — footer now sits cleanly after "Add a herd" with normal spacing, no overlap, regardless of how many herds someone has.
**Revisit if:** None expected — this restores the drawer to a single consistent in-flow list, which is the more robust default as the herd list grows.

## 2026-08-15 — Browse: Batch 2, Providence, RI (7 venues)

**What:** Added a second Browse venue batch to `app/data/venues.js`: AS220 (Tiny venues), Columbus Theatre (Small venues, Classic theatres), Fete Music Hall (Small venues), The Met — Pawtucket (Small venues, Legendary), Providence Performing Arts Center (Classic theatres), Amica Mutual Pavilion (Arenas & Stadiums, Legendary), and Strand Ballroom & Theatre (Small venues, Classic theatres, Legendary). New `metro: 'Providence'` value, so the By State/Region chip strip now shows two regions. Every fact web-verified (Wikipedia, each venue's own site, or a specific verifiable listing) — same standard as Batch 1. Left `capacity`/`established`/`transit` as `null` rather than guessing wherever no citable number existed: Fete Music Hall's capacity (sources disagreed — one said ~900, another an unhelpful "100+" — not confident enough to publish either), The Met's capacity and opening year, and `transit` for the whole batch (Providence has no MBTA service, so it's genuinely not applicable rather than an oversight).
**Why:** Requested directly — "no it ws not for search it was browse okay lets just make it anew withe the venue.js guidance," after confirming no larger prior venue list ever existed to recover. Followed the exact schema/rigor already established for Batch 1 (Boston/Cambridge).
**Notable data-integrity call:** The rirocks.net-sourced Lupo's Heartbreak Hotel history (Ramones, Talking Heads, Muddy Waters, Stevie Ray Vaughan, etc.) does **not** appear as `notableShows` on either The Met or Strand Ballroom & Theatre, even though both trace back to that lineage. Lupo's Heartbreak Hotel operated out of three different buildings over the decades (Conrad building 1975–93, Peerless building 1993–2003, then the building that's now Strand Ballroom & Theatre 2003–2017) — and separately, The Met's own site describes itself as "a mix" of Lupo's and the old Met Cafe, in a fourth, different building. Attaching those specific historic shows to either current address would very likely misattribute them to a building that wasn't yet hosting music when those artists played. Strand's entry has no `notableShows` at all; The Met's has one general, source-backed lineage note instead of specific dated shows.
**Verified:** Playwright — Region chip strip now shows "Boston/Cambridge" and "Providence," selecting Providence returns exactly 7 venues, no console errors, cards with a null capacity correctly omit the "Cap." field rather than showing a wrong number.
**Revisit if:** The remaining New England metros (Portland ME, Burlington VT, New Haven/Hartford CT, Manchester NH) are still unbuilt — same next-step as before, just one metro closer. If the capacity-band gap (venues like PPAC ~3,100 and Strand ~1,700, between the Small and Arenas & Stadiums bands) comes up again across future batches, worth deciding whether "Classic theatres" is doing enough work as the de facto mid-size category or whether the size bands themselves need a rethink.

## 2026-08-15 — Browse: triangulated Batches 1–2, added Batches 3–6 (Portland, Burlington, New Haven/Hartford, Manchester) — 57 venues total

**What:** Two things in one pass, per direct request ("can we try to triangulate the list too? ... lets build it all out"):

1. **Triangulation of the existing 14 venues** (Boston/Cambridge + Providence) — every fact re-checked against a second independent source. Concrete corrections made:
   - **Columbus Theatre → renamed to Uptown Theater.** The venue closed permanently under that name in June 2024; new ownership reopened the same building (270 Broadway, Providence) in 2025 as "Uptown Theater." `columbustheatre.com` still resolves and shows listings but is a stale page — updated `url` to `uptownpvd.com` and corrected the `established` comment, which had the Columbus/Uptown naming history backwards (opened as Columbus Theatre in 1926, was Uptown Theatre 1929–1962, reverted to Columbus Theatre until the 2024 closure, now Uptown Theater again).
   - **MGM Music Hall at Fenway's `allAges`** was wrong — corrected from `'18+ (varies by event)'` to `'all ages (varies by event)'`; two independent sources confirm the venue's default policy is all-ages, not 18+.
   - **Paradise Rock Club** — removed "Tom Waits" from the Wall of Fame notable-show claim; only Pixies and A Tribe Called Quest could be independently confirmed as being on the actual Wall of Fame.
   - **Providence Performing Arts Center** — removed Aerosmith and Van Morrison from the "hosted during 1973–74" claim; a second source's 1973–74 rundown doesn't include either, so kept only the acts confirmed by two sources (Bee Gees, The Doors, Queen, Fleetwood Mac).
   - Flagged (not changed): AS220's capacity is single-sourced (venue's own site only, no second source found) — left as-is with a comment noting the lower confidence, rather than nulling a plausible, official-source figure.
   - Everything else in the original 14 checked out clean against a second source — no other changes needed.

2. **Four new metro batches added**, same schema and rigor as Batches 1–2: Portland, ME (11 venues), Burlington, VT (10), New Haven/Hartford, CT (12), Manchester, NH (10, including nearby Derry/Concord/Nashua — same "hub metro, real nearby towns" pattern Boston used for Cambridge). **57 venues total across 6 metros.** Each entry's research agent was instructed to prefer a venue's own site plus one independent source, avoid low-reliability AI-aggregator sites, and leave `capacity`/`established`/`transit`/`notableShows` null or omitted rather than guess — a meaningful number of entries across all four new batches have `capacity: null` for exactly that reason (e.g. Toad's Place, Nectar's, Angel City Music Hall, Space Ballroom, Cafe Nine). `transit` is `null` across all four new metros — none of them have subway/light rail service, so it's a genuine "not applicable," not an oversight.

   A handful of venues were researched and deliberately **excluded** as not meeting the verification bar or being effectively closed/inactive: Port City Music Hall and Empire (Portland, both closed/rebranded), 242 Main, Club Metronome, and ArtsRiot (Burlington, all closed), The State House and Fairfield Theatre Company (New Haven area — the former closed, the latter isn't really this metro), and Anheuser-Busch Merrimack Biergarten / Stark Brewing Co. / BankNH Pavilion (Manchester — either not primarily music venues or a genuinely separate market).

**Why:** Requested directly. The original "60–100, maybe more" venue-count estimate from early planning was always a rough target, not a hard commitment — 57 is where the list landed once "don't pad the count with venues you can't verify" (also requested directly) was applied consistently across four new metros. A few markets (Portland, Manchester) simply don't have as many independently-verifiable live-music venues as Boston does.
**Verified:** Playwright — Region chip strip now shows all 6 metros; selecting each of the 4 new ones returns the exact expected venue list (Portland 11, Burlington 10, New Haven/Hartford 12, Manchester 10); Size and Type views both render against the full 57-venue set with no console errors; a spot-check screenshot of the By Type view (Club Passim/Scullers/Lizard Lounge) confirms cards still render correctly with the corrected data.
**Revisit if:** If venue counts still feel thin in any one metro later, the excluded-venue lists above are the first place to look — most of those were excluded for being closed or off-topic, not because research came up empty, so a fresh look might turn up a legitimate replacement. Fete Music Hall (Providence) and Angel City Music Hall (Manchester), among a few others, still have `capacity: null` because two research passes turned up nothing citable — worth a fresh check periodically in case an official number gets published later.

## 2026-08-15 — Boston/Cambridge: added the big/iconic venues (Sinclair, Xfinity Center, South Shore Music Circus, Cape Cod Melody Tent, Gillette Stadium, Fenway Park)

**What:** Added 6 venues to the Boston/Cambridge batch: The Sinclair (Cambridge, Small venues), Xfinity Center (Mansfield — Arenas & Stadiums, Outdoor, Legendary), South Shore Music Circus (Cohasset — Outdoor, Legendary), Cape Cod Melody Tent (Hyannis — Outdoor, Legendary), Gillette Stadium (Foxborough — Arenas & Stadiums, Outdoor), and Fenway Park (Boston — Arenas & Stadiums, Outdoor, Legendary). Boston/Cambridge goes from 7 venues to 13. Total across all 6 metros is now **63**.
**Why:** Requested directly — "do you have sinclair? and comacst center? and cohasset music circus and cape code melody tent and gilette and fenway? ... just want to make sure we're really covering the big ones around here thats key." A fair catch: the original Batch 1 (7 venues) never got revisited once the newer metros started, so Boston/Cambridge — the flagship, home-base metro — was thinner than several of the newer ones. South Shore Music Circus and Cape Cod Melody Tent are a real drive from Boston proper, but both are the same operator's tent-theatre pair and functionally part of the same touring circuit (artists often play both back-to-back) — kept under `metro: 'Boston/Cambridge'` rather than inventing a separate Cape Cod metro for two venues, same "hub metro, real venues further out" pattern already used for Cambridge/Allston in this same batch and Derry/Concord/Nashua under Manchester.
**Notable capacity calls:** Fenway Park's `capacity` uses the concert attendance record (37,200, Lady Gaga 2022) rather than the ballpark's baseball-game seating figure, since concert configuration is meaningfully different and that's what this app's users care about. Gillette Stadium's `capacity` similarly uses its own record concert attendance (71,723, Ed Sheeran 2023) rather than a fixed seating number, since stadium concert capacity varies by staging. The Sinclair has no `capacity` or `established` — not published on the venue's own site, and no independent source turned one up.
**Verified:** Playwright — Boston/Cambridge region filter now returns all 13 venues by name, no console errors.
**Revisit if:** The Sinclair's capacity/opening year should be filled in if a reliable source ever turns one up (it's a well-known, frequently-booked room — this is a real gap, not a "genuinely unpublished" situation like Fete Music Hall's).

## 2026-08-15 — Boston/Cambridge: deep sweep adds 19 more major venues (82 total); fixed a real name collision; nav spacing fixed for real this time

**What:** Three things:

1. **Deep sweep of Boston/Cambridge**, per direct follow-up ("but there are more" → confirmed which ones with "did you get royale? and bull run? and tupelo music hall up in nh? and shalin liu?"). Added 19 more venues: TD Garden, Leader Bank Pavilion, The Stage at Suffolk Downs, Wang Theatre, Shubert Theatre (Boston), Orpheum Theatre, Symphony Hall, Berklee Performance Center, The Wilbur, Somerville Theatre, Chevalier Theatre, Regent Theatre, House of Blues Boston, Roadrunner, The Middle East, Royale, City Winery Boston, Bull Run (Shirley), and Shalin Liu Performance Center (Rockport). Same verification standard as every other batch. Tupelo Music Hall was already covered (it's in the Manchester, NH batch — the venue itself is in Derry, NH). Boston/Cambridge goes from 13 to 32 venues; **total across all 6 metros is now 82.**
2. **Caught and fixed a real name collision**: the new Boston Shubert Theatre and the existing New Haven Shubert Theatre (Batch 5) are two unrelated venues with the exact same name. Since Favorites are keyed by venue display name (a known limitation — see the Favorites entry above), this would have let favoriting one silently collide with the other. Renamed both to `Shubert Theatre (Boston)` and `Shubert Theatre (New Haven)`.
3. **Actually fixed the drawer nav spacing** — the first pass (see the "Drawer: fixed nav item spacing" entry above) made My Shows → Browse Venues geometrically equal to every other 8px gap in the drawer, but it still read as tight, per direct follow-up ("the spacing issue in the nav is still off between venues and my shows"). The real issue was optical, not geometric: an 8px gap between two solid, same-color filled rows has much higher contrast than the same 8px gap between a filled row and a plain text label (Browse Venues → "HERDS"), so identical spacing didn't look identical. Bumped the gap specifically between My Shows and Browse Venues to `--space-16` (there's no `--space-12` in the token scale, so this takes a full step rather than an off-scale value) — scoped to `#drawer-my-shows-btn`'s own margin, not the shared `.drawer-my-shows-btn` class, so Browse Venues → Herds and the Browse landing page rows (which didn't have this problem) are untouched.
**Why:** Requested directly, in three follow-up messages across this session.
**Verified:** Node script confirms 82 venues, zero duplicate names, no bad categories, no missing required fields. Playwright screenshot of the drawer (with fake herd rows injected) confirms the corrected spacing reads as intentional, not just numerically equal.
**Revisit if:** None expected for the spacing fix. For the venue list, worth periodically re-checking whether any other city's batch has grown into a name collision as new metros or venues get added — the Shubert Theatre collision here is a preview of a class of bug this schema is exposed to as long as Favorites key off display name rather than a stable id.

## 2026-08-15 — Browse venue cards: cut the "+N more" counter next to notable shows

**What:** Venue cards show only the first `notableShows` entry (unchanged) — but used to append a "+N more" counter right next to it (e.g. "...before they were well known +2 more"). Cut the counter; the card now just shows the one fact and stops.
**Why:** Called out directly — "why does one show fact say and two more... that sounds weird one fact i enough - i think its paradise rock club." Fair: tacking a bookkeeping count onto a single piece of trivia read odd, and the card was already deliberately choosing to show just one fact rather than a running list — the counter undercut that choice by announcing what it was leaving out.
**Verified:** Playwright — every card with `notableShows` now renders exactly one fact, no "more" text anywhere in the rendered output.
**Revisit if:** None expected — this is a straightforward cut, not a tradeoff.

## 2026-08-15 — Drawer nav spacing: reverted the "optical" fix, back to a genuinely uniform 8px

**What:** The previous pass bumped just the My Shows → Browse Venues gap to `--space-16` as an optical correction (reasoning: a gap between two filled rows reads tighter than the same gap next to plain text). Per direct follow-up ("the spacing is still not right in the nav the vertical spacing it should be uniform"), that was the wrong call — reverted it. Every gap in the drawer nav column (My Shows → Browse Venues, Browse Venues → Herds label, and every herd row) is back to a single, literal `--space-8` with no exceptions.
**Why:** Requested directly, twice — first that the spacing looked off, then explicitly that it should be uniform once the "optical" fix didn't land. Confirmed via direct pixel measurement this time (not just a screenshot read) that all four gaps in the drawer are exactly 8px.
**Revisit if:** If this still doesn't look right, the "optical" theory from the previous entry was probably wrong to begin with — worth asking what specifically still looks uneven rather than guessing at another CSS tweak.

## 2026-08-15 — Typography pass: venue cards cleaned up, page-title sizes unified

**What:** A focused typography pass, per direct feedback ("typography needs to be addressed across the site... venue cards - so many different varying sizes... make sure we're using tokens"). Found that every `font-size` in the codebase already referenced a token (no raw px values anywhere — confirmed by grep) — the real problem was too many *different* tokens applied to elements playing the same visual role, not missing tokens. Two concrete fixes:

1. **Venue cards** (`app/scripts/browse.js`, `app/styles/browse.css`):
   - Removed the transit/nearest-T-stop line entirely ("remove the train guidance it's superfluous"). The `transit` field stays in `venues.js` (still real data), it's just not rendered on the card. Removed the now-unused `train-front` icon from `scripts/icons.js`.
   - Removed the all-ages chip from the meta line ("remove the all ages tag") — most venues in this directory default to all ages anyway, so it was mostly noise.
   - Removed the border-top rule that set the notable-fact line off from the rest of the card ("remove the stroke over the fun fact and just make that stand out with the icon"). Added a `sparkles` icon in front of the fact instead (new entry in `scripts/icons.js`, pulled from the same `lucide-static@1.29.0` pin as every other icon) — same "icon carries the meaning, not a divider line" language the legendary-star marker already uses.
   - Unified the card down to two font sizes total instead of three: `--text-sub-size` for the venue name, `--text-control-size` (14px) for every line underneath it (address, meta, notable fact) — `.venue-meta` was `--text-meta-size` (12px), bumped up to match. `.venue-notable`'s line-height was also a bare `1.4` instead of a token — fixed to `--text-control-lh`, so it's a genuine match now, not a coincidentally-equal font-size with different rhythm ("make that the same type size as the other details, just want to make sure we're not going too small" — 14px is the floor now for anything meant to read as a real sentence).
2. **Page titles**: `.group-heading-name` (the herd-selected / My Shows page header) was `--text-sub-size` (20px) while `.browse-heading` (Browse Venues' own page header) was `--text-head-size` (24px) — two different sizes for the same "title of the page you're on" role. Matched `.group-heading-name` up to `--text-head-size` rather than shrinking Browse Venues down. This also moves `#my-shows-heading-row`, which reuses the same class.
**Why:** Requested directly, as part of a broader "typography needs to be addressed across the site" callout.
**Verified:** Playwright — venue cards render with no all-ages chip, no transit line, a sparkles icon on the notable-fact line with no border above it, and matching 14px type throughout the detail lines (checked via rendered HTML, not just a screenshot read). Herd-name page header confirmed at the larger size via screenshot. No console warnings (icon set resolves cleanly with train-front gone and sparkles added) and no page errors.
**Revisit if:** This was scoped to venue cards and page titles specifically, since those were the two concrete things named. If other spots still feel inconsistent (nav drawer, show cards on the main list, etc.), worth naming them specifically for a follow-up pass rather than re-auditing the whole site again from scratch.

## 2026-08-15 — Zero-herd landing: a chunky three-action "placemat" instead of a dead end

**What:** Replaced the old bare "No groups yet." empty state (icon + one line of text, no way forward except the drawer) with a real landing view for a brand-new person: three big, equal-weight tiles — **Browse Venues**, **Add a Show**, **Start a Herd** — stacked vertically on mobile, side by side in a row on desktop (1024px breakpoint, matching `grid.css`'s existing row-of-three convention). Each tile is a real surface card (same language as `.add-show-card`) with a big icon circle, bold label, and one line of sub-copy.
- **Browse Venues** reuses the drawer's existing `#drawer-browse-btn` click handler by just triggering it — this also incidentally fixes a "Revisit if" flagged all the way back in the original Browse merge entry: Browse Venues used to be completely unreachable for a zero-herd person, since it only ever lived inside `#group-bar`, which stays hidden until you have at least one herd.
- **Start a Herd** opens the existing new-herd modal, unchanged.
- **Add a Show** is the one genuinely new behavior: it auto-creates a herd named `"{their name}'s Shows"` and drops the person straight into the real Add Show flow, no naming step in between.
**Why:** Requested directly — "now that we have browse venues, i think we can make a nice placemat for people just landing here so its not like a dead end... it can be three actions browse venues add a show and start a herd... chunky though. maybe stacked vertically on mobile but left to right on desktop." The "Add a Show" behavior needed a real product decision first, since every show in the schema belongs to a herd — there's no standalone/solo tracking today. Talked through whether a solo path drifts from the app's own stated mission (`MISSION.md`: "overlap is the signal, not attendance") — landed on: it doesn't, because every herd already starts as one person before anyone else joins. Auto-creating a herd-of-one on "Add a Show" isn't a new mode, just skipping the up-front naming ceremony to get to that same starting point faster. Confirmed directly before building.
**Verified:** Playwright, with a mocked Supabase client — all three tiles render at zero herds; Browse Venues tile opens `#browse-view`; Start a Herd tile opens the new-herd modal; Add a Show tile creates a group named "Dan's Shows", lands on that herd's page, and opens the Add Show panel already expanded. Checked both the stacked-mobile (420px) and row-desktop (1200px) layouts by screenshot.
**Revisit if:** The auto-generated herd name (`"{name}'s Shows"`) is a first guess at friendly default copy — worth watching whether real usage suggests something better once this ships. Also worth eventually deciding whether a "renamed later" herd like this should look/behave any differently from a herd someone deliberately named and invited people to from the start — right now it's identical in every way, which was the whole point (no new solo mode, just a faster path into the same one).

## 2026-08-15 — Page-title font treatment unified; solo herd presents as "My Shows"

**What:** Two related header fixes:
1. **Font treatment**: `.group-heading-name` (herd page + My Shows page title) now uses the exact same typographic treatment as `.browse-heading` (Browse Venues' title) — `font-family: var(--font-display)`, weight 900, uppercase, `letter-spacing: 0.01em` — instead of body font at weight 600 with capitalize. All three page titles now look like one consistent style.
2. **Solo herd = "My Shows"**: When a person has exactly one herd (`myGroups.length === 1`), that herd's page heading now reads "My Shows" instead of its literal name (e.g. "Dan's Shows") — the old behavior showed a name that just duplicated what the My Shows tab already said. But the herd page's Share/View buttons do a real invite/member-list underneath, and leaving them there under a "My Shows" heading would be misleading (inviting someone doesn't add them to "my" anything — it makes a real shared herd). Resolved by swapping those two buttons out for a single "+" icon (new `#group-add-herd-btn`, new `plus` icon in `scripts/icons.js`) that opens the same new-herd modal as the zero-herd landing's Start a Herd tile. The moment a second herd exists, `selectGroup()` reverts automatically to the herd's real name plus the normal Share/View buttons — this is purely a display state driven by `myGroups.length`, not a stored flag on the herd itself.
**Why:** Requested directly — "headers are still not aligned my shows, browse venues herd header. I like the brows[e] treatment lets just do that. 'user's' shows is duplicating my shows. can the first 'herd' just be 'users' shows presented as my shows?" The solo-herd relabel raised an immediate follow-up concern once discussed: "ah but you can't invite people to 'my shows' if someone clicks invite then its a make a herd flow." Asked directly how to resolve that tension (relabel the button vs. always show the real herd name vs. hide invite entirely) — chose a fourth option the user proposed in response: swap Share/View for a plain "+" that goes straight to Start a Herd, rather than trying to relabel an invite action to fit under a "My Shows" heading.
**Verified:** Playwright with a mocked Supabase client, two scenarios. Solo (one herd): heading reads "My Shows", Share/View buttons `display: none`, "+" button visible and opens the new-herd modal on click, heading font-family/weight/text-transform confirmed as Big Shoulders Display / 900 / uppercase via `getComputedStyle`. Multi (two herds): heading reads the real herd name, Share/View buttons visible, "+" button `display: none`. No console/page errors in either case.
**Revisit if:** This is a display-time decision (`myGroups.length === 1`), so it's inherently a bit fragile to future changes in how herds are created or left — if a "rename this herd back to its real name manually" feature is ever added, this logic would need a real stored flag instead of inferring "soloness" from group count.

## 2026-08-15 — Design system drift check (ds-tools)

**What:** Ran the `ds-tools` skill's discipline loop against the live app (no npm project exists here — it's a no-build vanilla HTML/CSS/JS app — so the snapshot/lint were done as a one-off Node script against `tokens.css` + `app/styles/*.css` + `index.html`, rather than via `npm run ds:snapshot`/`ds:lint`). Found and fixed two real violations:
- `browse.css` `.venue-fav-btn svg` was hardcoded to `20px` — happened to be the exact value of `--icon-size`, just not written as a reference. Switched to `var(--icon-size)`.
- Documented (not changed) three spots where a token's exact value is hardcoded out of necessity, not drift, each now marked `ds:intentional` with an explanatory comment: the `<meta name="theme-color">` tag (can't reference a CSS custom property from HTML), the `AVATAR_COLORS` "Paper" swatch and the `contrastTextFor()` fallback colors in `index.html` (JS can't read CSS custom properties without extra plumbing; both already had prose comments explaining the reuse, now also machine-flaggable/suppressible). Also flagged `grid.css`'s debug-only grid-overlay red as intentional (never ships, deliberately off-palette).
**Why:** Asked directly — "do we still have that design system drift check skill? ... yea lets run it" — as a maintenance pass after several rounds of typography/spacing work this session.
**Not fixed, left for a human decision (ds-tools opportunities, not violations):**
- `.chip-strip-item svg` in `browse.css` is `16px` — coincidentally equal to `--space-16`'s value, but this is an icon size, not a spacing usage, so it's not really "the same token" — more likely a deliberate smaller icon variant next to the standard 20px (`--icon-size`) one used elsewhere (venue-fav, drawer icons). Worth deciding: add a second icon-size token (e.g. `--icon-size-sm: 16px`) if this size is going to recur, or leave it be if it's a one-off for filter chips.
- Underused tokens (1–2 uses in live files): `--space-56`, `--color-accent`, `--color-scrim`, `--text-head-size`.
- Zero-use tokens (no `var()` reference found in the live CSS/HTML scanned): `--space-0`, `--space-48`, `--space-72`, `--color-secondary`, `--text-display-size`, `--text-display-lh`. (`--color-secondary` — army green — and the display-size pair may just not have a use case yet; worth confirming before treating as dead weight.)
- Duplicate-value token pairs sharing a raw value for different semantic roles (expected, e.g. `--space-8`/`--radius` both being 8px) — none of these looked like accidental drift on inspection, so left alone.
- Type-scale clustering: `--text-meta-size` (12px) and `--text-control-size` (14px) are 2px apart, as are `--text-control-size` and `--text-body-size` (16px) — both already have detailed comments explaining why they're deliberately close (meta vs. tappable-control register, control vs. reading-body register), so this reads as intentional density steps, not drift.
**Verified:** Re-ran the lint after the two fixes — down to the single flagged `chip-strip-item` icon-size question above; loaded the app headlessly (mocked Supabase, `pageerror` listener) after the changes, no console errors.
**Revisit if:** This app has no `package.json`/build step, so there's no persistent `npm run ds:lint` wired up yet — the one-off script lives only in this session's scratch space. Worth deciding whether it's worth adding a minimal `package.json` with `ds:snapshot`/`ds:lint` scripts (per the skill's own setup guidance) so this becomes a rerunnable command instead of something that has to be reconstructed each time.
